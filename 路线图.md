# 路线图：整体进度与剩余工作

> 最后更新：2026-09-09

---

## 一、总进度：约 19%

权重按**实际工作量**估计，不是按条目数。
比 LOL 那个项目的 18% 低一半，原因很简单：**那边的数据是请求来的，这边的数据要自己造。**

| 阶段 | 权重 | 完成度 | 贡献 |
| --- | ---: | ---: | ---: |
| 数据采集链路（录像 → 帧 → 观测 → State → 核对） | 20% | 57.5% | 11.5 |
| 小地图识别（位置信息） | 10% | 45% | 4.5 |
| 数据规模化（标注效率、批量、去重） | 15% | 10% | 1.5 |
| V(s) 胜率模型 | 12% | 0% | 0 |
| 胜率曲线与展示 | 3% | 0% | 0 |
| **行为抽象**（把连续操作切成可比较的离散动作） | 20% | 5% | 1.0 |
| **Q(s,a) 因果估计** | 15% | 0% | 0 |
| Regret 与归因 | 5% | 0% | 0 |
| **合计** | **100%** | | **18.5（约19）** |

「数据采集链路 57.5%」指的是：代码结构、网页录像来源、远程流式读取、经济与通识参考、存储、校验、标注、评估、标定界面都做完了，
但**没有一场真实比赛进过系统**，OCR 准确率未知，HUD 标定结果是空的。
剩下的 45% 主要是「拿真实数据打磨」，那部分往往比写代码久。

「小地图识别 45%」：认位置做完了（合成图 10/10），**认英雄一点没做**。
后者是剩下的 55%，难度未知 —— 有可能做不出来。

「行为抽象 5%」只是定义了事件类型（拿暴君、推塔、击杀）。
**事件不是行为。**「12 分 30 秒入侵敌方蓝区」才是行为，那个还没开始。

---

## 二、和 LOL 项目的进度不能直接比

LOL 那边 18% 里，有 12 分来自「数据管道」—— 而那条管道的核心是
两个 HTTPS 请求。这边同样的位置站着一个完全没解决的问题：
**怎么把画面变成可信的数字。**

换句话说：LOL 项目的剩余工作主要是**建模**，
这边的剩余工作里有一大块还是**获取数据**，而且这块没有现成方案可抄。

---

## 三、剩余工作与阻塞关系

**【你】**= 只有用户能做；**【AI】**= 代码工作。

### 你0 · 人工标 10~15 帧 【你】· 约 40 分钟 ← **卡住全部后续工作**

零依赖，不用装任何东西：
```
python3 -m kplab serve
```

产出三样东西，第三样最重要：
1. 第一批完全可信的数据
2. 评估 OCR 的 ground truth
3. **「一场要标多久」的真实数字** —— 它决定这个项目的规模上限

### 你1 · 等待5条官方原文歧义获得明确文本 【资料】
26条S44官方说明中已有21条可计算；剩余5条因原文歧义或缺具体数值暂不参与计算。
> 需先完成：无（随时可做）

### AI1 · 用真实体感重估规模 【AI + 你】· 半天
见第四节那张表。**这一步可能会推翻整个项目的技术路线**，所以要早做。
> 需先完成：你0

### AI2 · 小地图英雄识别 【AI】· 难度未知，1 周~不可行
最大的技术缺口。建议先做粗版本（认颜色和数量，不精确定位）。
> 需先完成：你0（要有标注过的小地图当训练/验证样本）

### AI3 · HUD 标定界面 + 跑通一次 OCR 【AI】· 2~3 天
后端接口有了，拖框的界面还没做。做完才能真正测出 OCR 准确率。
> 需先完成：你0（要有 ground truth）

### AI4 · 一版「丑但是真」的 V(s) 【AI】· 1~2 天
用现有的少量数据 + 最简单的模型，**明知道它不准**。
目的是把整条链路打通、看到产品长什么样，在投入大量标注之前。
和 LOL 路线图的 AI2 是同一个思路，也是同样最降风险的一步。
> 需先完成：你0

### 你2 · 定义「行为」到底是什么 【你定义 + AI 实现】· 反复迭代 1~2 周
「入侵敌方野区」怎么从坐标和事件里定义？多长时间算一次？
失败的入侵和成功的入侵算同一个动作吗？
**这些没有标准答案，必须由懂游戏的人来定。**
> 需先完成：AI4（看过真实曲线之后才知道该定义什么粒度）

### AI5 · Q(s,a)：从相关性走到因果 【AI】· 2~4 周，研究性质
不能简单拟合「做了 A 的人赢了多少」—— 那学到的是「优势方才有余裕做 A」。
**数据量解决方差，不解决偏差**，这一步再多数据也绕不过去。
而且这边的数据量注定远小于 LOL，方差问题会更严重。
> 需先完成：你2 + 足够的样本

---

## 四、硬约束：这边的瓶颈是人，不是带宽

**LOL 那边的瓶颈是 API 限流**（1500 场/小时，是挂机时间，可以和别的工作并行）。
**这边的瓶颈是人工标注**（无法并行，无法挂机，会累）。

按一场 40 帧、每帧十几个字段估算：

| 目标场数 | 纯人工标注 | 现实吗 |
| ---: | ---: | --- |
| 10 | 约 5 小时 | ✅ 可行，够跑通链路 |
| 100 | 约 50 小时 | ⚠️ 两周全职，勉强 |
| 1,000 | 约 500 小时 | ❌ 不可行 |
| 10,000 | 约 5,000 小时 | ❌ 不用讨论 |

**这张表是整个项目最重要的一张。** 它说明：

1. 照抄 LOL 的「采几万场再训模型」思路在这边**根本走不通**
2. 要么 OCR 做到足够准（把人工从「填数字」降级成「抽查」），
   要么接受小样本，转向更强先验的方法
3. 所以 **AI3（跑通 OCR）的优先级实际上高于任何建模工作** ——
   它决定的是这个项目有没有规模化的可能

观测和 State 压缩后不到 1 MB，但高密度帧图不能再假定“几乎不占空间”。
智能模式一场通常会留下数百张 JPEG，实际大小要用真实录像测量；按每场30~120 MB
粗算，一千场会到30~120 GB。后续必须实现帧图生命周期管理：完成识别和人工核对后，
只长期保留观测、关键帧和抽样审计帧，不能无限积累全部帧图。

---

## 五、总时间：不给估计

LOL 那边敢写「6~10 周」，因为它的不确定性集中在最后一步（因果 Q）。

这边的不确定性在**第一步**：小地图识别做不做得出来、OCR 准不准、
一场到底要标多久 —— 这三个数字现在一个都不知道，
而它们决定的不是「快慢」，是「可行不可行」。

**等你0 做完，才有资格给时间估计。** 现在给的任何数字都是编的。

---

## 六、给 AI 的提醒

- 不要因为「代码写完了」就以为项目过半 —— 零真实数据，就是 10%
- 不要在没有人工标注的情况下相信任何 OCR 输出
- 不要把「没读到」写成 0，那是最难发现的一类错误
- 不要用后面的帧回头修前面的帧，即使曲线会更好看
- 不要用「拟合 P(赢 | state, action)」来交付 Q(s,a)，那是相关性不是因果
- 不要照抄 LOL 路线图里的场数规模 —— 这边的瓶颈是人不是带宽
