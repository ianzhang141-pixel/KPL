"""王者荣耀 决策实验室 —— 数据验证与状态抽取工具箱（kplab）。

这个包是 LOL 项目 `lolab` 的姊妹项目，但**不是移植**。

设计原则（前四条直接继承自 LOL 项目踩过的坑）：

1. 只用 Python 标准库，Mac 自带 python3 即可运行，不需要 pip 安装任何东西。
   可选的抽帧 / OCR 能力依赖外部程序（ffmpeg 等），装了就用，没装就明说，
   **绝不因为缺工具就编造数字**。
2. 原始观测一律落档，而且从第一天就压缩存。LOL 项目吃过的亏：
   GPT 那版把原始 Timeline 用完就丢，改一次 State 定义就要全部重下。
3. 严格无未来信息泄漏：t 时刻的 State 只能用 t 时刻及之前的画面。
4. 本包内**不存在**任何假装训练好的 V(s) / Q(s,a) 模型。

第 5 条是王者荣耀独有的，LOL 不需要：

5. **每一个字段都必须带来源和置信度。**
   LOL 的数字来自 Riot 官方 API，是事实；王者荣耀的数字来自对录像画面的
   识别，是推断。推断可能是错的，所以 State 里每个值都要说清楚
   「这是人工看着填的，还是机器识别的，识别有多确信」。
   把识别结果当事实呈现，是这个项目最容易犯、后果最严重的错误。
"""

__version__ = "0.9.3"
