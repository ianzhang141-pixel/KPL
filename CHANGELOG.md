# 变更日志

> **给 AI：每次改动都必须在最上面加一条，并同步更新 [项目状态.md](项目状态.md)。**
> 格式：日期 + 做了什么 + 验证到什么程度 + 刻意没做什么。
> 最新的在最上面。

---

## 2026-09-09（第九轮）· 队伍色变成硬条件、分配求精确最优、分不清就不写号码

用户拿真画面截了 5 张图，报了四件事：还是圈到空地和龙坑、把回城光柱当成头像、
**红蓝混认**、以及头像认错人。前三件都指向同一处设计缺陷。

### 根因：队伍颜色被当成了加分项

小地图上每个英雄标记外面都有一圈队伍色，这是整套识别里**最可靠**的一条约束。
而旧代码只要求候选框里有 1.2% 的「任意队伍色」，同队颜色最多加 0.025 分 ——
等于没有约束。于是蓝方槽位可以心安理得地认下红方图标，空地也能过关。

改成**硬条件**：图标外圈上本方队伍色要够（默认 6%）**且多于敌方**，否则直接拒绝。

而且**只看外圈，不看整块**。中间那张脸本来就可能是任何颜色 ——
一个红方英雄的原画偏蓝，整块里的蓝像素就能压过外圈的红。
实测正是这样：改成只采半径 0.42~0.57×边长 那一圈之后，
本方色从 0.00/0.39 这种自相矛盾的读数变成了干净的 0.28~0.46，敌方色几乎全是 0。

### 回城光柱：区别不在颜色，在大小

紫色光柱（约 R170 G60 B230）蓝−红 有 60，默认门槛 40 就把它放进来了，
一整条变成「蓝方颜色」。新增 `marker_mask()`：只保留**大小像一个英雄标记**的
连通域，用的就是标定页上已有的「一团最少/最多多少像素」，不另加阈值。

关键是超大的那些**不抹成 0，而是标成 OVERSIZE**。
「这里没有队伍色」和「这里被特效糊住了」是两回事：前者说明没人，
后者只说明这一帧的颜色证据用不了。一个英雄的圈如果和光柱连在一起，
直接丢掉就等于把人也丢了 —— 现在改成对这种候选**抬高分数门槛**（+0.05）而不是否决。
合成图上正是靠这条把 1 号救了回来。

页面里也写清了更直接的一招：**把「蓝要比红多」调到 60~90**。
真正的蓝色标记这个差值通常远大于 100，调高挡掉紫色不会伤到英雄。

### 头像错认：贪心分配 + 对齐噪声

两处：

1. **贪心分配会连环出错。** 分数最高的先挑走图标，被抢的槽位只好去认第二像的东西。
   用户图 4、5 里蓝1、蓝5、红9 同时错位就是这样来的。改成**每队 5 个槽位做
   32 个状态的动态规划，求精确最优的一对一分配**（`assign_one_to_one()`），
   不引入任何库，并和暴力枚举对拍过 120 组。
2. **每个槽位各自微调落点，再拿各自的分数互相比较。** 两个一模一样的头像
   也能差出 0.05 —— 那不是区分度，是对齐噪声。改成**在同一块像素上给每个槽位重新打分**。

### 分不清是谁，就不写号码

新增：把这一对禁掉之后，最优总分掉得少于 0.02，就说明**换谁都一样**。
这时输出 `slot: null` + 队伍，页面上画一个黄色虚线的「?」——
**「蓝方有个人在这儿」是真的，「他是几号」不装。**

已知损失：观测记录按槽位建索引，没号码的点暂时进不了 `players`。
这写进了 `to_observation_players()` 的文档里 —— 要留住它得先改观测结构，
但绝不能为了塞进来而随便安一个号码。

### 合成画面实测（在原图上加了回城光柱和守卫小色点）

| | 认对 | 圈到空地/龙/光柱 | 红蓝混认 |
|---|---|---|---|
| 上一轮 | 8/10 | 有（跨队认了 1 个） | 有 |
| 这一轮 | **9/10** | **0** | **0** |

漏掉的那一个是配色和地图底色接近、自匹配分只有 0.70 的深色英雄，
体检表里如实写着「分数不够」，没有为它放宽门槛。

### 标定页

- 新增可调的「图标外圈的本方队伍色至少占」（默认 0.06），和相似度门槛并列。
- 体检表加了「本方色 / 敌方色」两列 —— 红蓝混认现在一眼能看出来。
- 新增状态：`wrong_team_colour`（最像的那块是**对方**颜色，已拒绝）、
  `ambiguous`（分不清是谁）。
- 说明里写清了紫色光柱为什么会被当成人，以及两个可用的调法。

### 验证到什么程度

- 测试 311 条全过（新增 12 条）。
- **变异测试 12 处**：队伍色变回加分项、门槛降回 0.012、采样圈挪到图标内部、
  不再区分本方敌方、超大团块直接抹掉、小色点也留下、分配退回贪心、
  不确定也硬写号码、一律说分不清、队伍色不再是硬条件 …… 全部导致测试失败。
  （另有 1 处防御性代码 —— 跨队争抢同一图标 —— 在颜色硬条件之后已不可达，如实记在这里。）
- `assign_one_to_one()` 与暴力枚举对拍 120 组，总分与唯一性均一致。
- Playwright 实测：体检表新增两列正确、图上 9 个带号码的点、无 JS 报错。单帧约 3.4 秒。

### 刻意没做

- **没做多帧联合**（相邻帧扣掉静止背景）。真正能再上一个台阶的是它。
- **没有为了凑够 10 个人调低任何门槛。**

---

## 2026-09-09（第八轮）· 小地图头像：召回 7→9，龙不再冒充英雄，十个槽位逐个体检

用户报：十个人只认出 4 个，而且**龙坑里的龙被认成了英雄**。

### 先造一把尺子，再改代码

真实录像不在仓库里，也没有逐帧的位置真值 —— 没有真值就没法说改动是变好还是变坏。
所以先画了一张**已知答案**的合成画面（`tests/test_minimap_portraits.py`）：
小地图上 10 个位置已知的英雄图标，两侧栏 10 个对应头像（更大、方形、四角是 UI 装饰），
外加一个**和英雄一样大、一样圆、一样有亮边框**的龙图标。

这张图不能外推分数（合成图案比真英雄干净得多），但能保证方向。
基线一跑就复现了用户说的两件事的机理。

### 三处根因

**1. 拿方形和圆形在比。** 侧栏头像是方的，四角是等级数字和边框；
小地图标记是圆的，四角是草丛和河道。旧代码把整个方形塞进 4×4 网格描述子，
**四角占掉整整 4 格**，而这 4 格在两边根本是不同的东西。
改成两边都只看内切圆（`circle_mask()`）。位置误差从 0.017 掉到 0.003。

**2. 一个槽位的六个备选，其实是同一个图标的六个偏移。** 粗搜步长 4px、四种尺寸，
同一个头像会生成一二十个几乎一样的候选，前六名全挤在一个人身上；
真正属于本槽位的图标连前六都进不去。加了**位置去重**，
备选必须是六个（现在十个）不同的地方。

**3. 圆形边缘分数被当成一票否决。** `radial_edge_score` 量的是图标内外的**颜色反差**，
它跟英雄配色和所在地形有关，和「他是不是个人」没有必然联系。
旧版 `circle < 0.42` 直接扔掉 —— 深色英雄站在深色野区，整类人凭空消失。
改成**弱边缘加价**：边缘越不清楚，要求头像本身越像（`circle_surcharge()`），
候选下限降到 0.24。

### 龙为什么会冒充英雄，以及怎么治

第 3 条放宽之后，合成图上立刻出现了用户报的那一幕：5 号找不到自己，
**分数第二高的是龙**，于是龙被贴上「蓝5」画在了图上。

治法不是把龙坑划成禁区 —— 龙坑正是最该看清有几个人的地方，
划禁区等于开龙团的瞬间集体失明。改成：**已经标定的塔和资源坑那一圈，
候选要多跨 0.06 分**（`STATIC_ZONE_MARGIN`）。已知那里蹲着一个长得像英雄的东西，
就该要求更强的证据。真有人站在龙坑上，头像匹配够强照样认得出来；龙本身过不去。

所以**必须先把暴君坑、主宰坑、风暴龙王候选坑框好**。没标的时候系统会明说
「龙的图标可能被认成英雄」，不再默默给出可能错的结果。

### 合成画面上的实测

| | 认对 | 位置误差 | 龙被认成人 |
|---|---|---|---|
| 改之前 | 7/10 | 0.017 | 否（但深色英雄也被一起扔了） |
| 只改匹配 | 9/10 | 0.003 | **是** |
| 匹配 + 标好资源坑 | **9/10** | **0.003** | **否** |

第 10 个人报「没找到」而不是指着龙说那是他 —— 这正是这个项目要的行为。

### 把仪表盘给人，而不是我拍脑袋定门槛

0.80 这个门槛**从来没有在真实录像上校准过**，我给不出正确值。所以：

- 门槛做成可调（`/api/status` 与 profile 里新增 `minSimilarity`，0.30~0.99），
- 「试一次」之后出一张**十个槽位的体检表**：最好分、需要多少、还差多少、卡在哪一步
  （没找到候选／分数不够／被分数更高的人占走／没有阵营颜色／在龙坑框里已抬门槛）。
- 只有「分数没到门槛」才标黄并提示可以调低；**被占走或压根没候选的不标黄** ——
  那种情况调门槛没用，标黄只会把人往错方向引。
- `save_thresholds()` 现在**校验类型和范围**。这份文件之后每一帧都读，
  写坏了不会报错，只会让后面每一帧悄悄识别失败 —— 那种错最难查。

### 验证到什么程度

- 新增 `tests/test_minimap_portraits.py` 15 条（含合成画面整条链路，需要 ffmpeg）。
- **变异测试 7 处**：去掉圆形掩码、圆度门槛退回 0.42、禁区不再加价、
  禁区不按中心过滤（侧栏头像也变成禁区）、去掉每槽位位置去重、
  圆外格子补平均值（凭空造出「和模板一致」的格子）、保存不校验 ——
  **7 处全部导致测试失败**。
- Playwright 实测：体检表十行齐全、图上圆点带槽位号、龙的位置没有圆点、
  「还差」不再出现负数、无 JS 报错。单帧识别约 3.4 秒。
- 全量 299 条测试通过。

### 刻意没做

- **没有为了凑够 10 个人而调低门槛。** 合成图上那个第 10 人现在是诚实的「没找到」。
- **没做多帧联合**（用相邻帧的静止背景扣掉地图）。那是下一个量级的改动，
  也是真正能再上一个台阶的方向。
- **合成图上的分数不代表真实录像。** 真实值只能你拿真画面在体检表上定。

---

## 2026-09-09（第七轮）· 52 个地标框之后的可用性：吸顶画面、分组折叠、龙坑照抄、版本号

用户说没看到大招框、技能框、龙王框和播报框。**这些 GPT 上一轮全都做了**
（`landmarkPlan()` 已经生成 52 个框，`hud.REGIONS` 里也有五个播报分区），
本机没 `git pull` 而已 —— 这已经是第三次因为同一个原因把「没同步」当成「没做」。

### 直接堵住这个误会

标题下面加了两个徽章：**当前版本号**（取自 `/api/status.version`）和
**「52 个地标框 · N 个项目框」**。看到 `v0.9.5` 和 52，就知道是最新的；
看到 26，就是没拉。不用再靠猜。

### 地标从 26 涨到 52 之后暴露的真问题

实测发现：从下面挑一个「召唤师技能」，画面早就滚出屏幕了，
要再滚回去才能拖框。这是数量翻倍带来的、上一轮不存在的问题。

- **左边整列吸顶**（宽屏，`position:sticky`）—— 挑框的时候画面一直在。
- **选中任何一项时，画面不在视野里就自动滚回来**（在视野里就不动，不乱跳）。
- **六个分组可折叠**（▾/▸），状态记在 localStorage。
  折叠只是列表收起来，和「隐藏框」是两回事，两者都不动坐标。

### 风暴龙王不用对着同一个坑拖两次

风暴龙王 20 分钟后**只会在上下两个龙坑之一**出现，事先不知道是哪个，
所以两个都得框；而这两个坑和暴君坑/主宰坑基本重合。
风暴龙王那一组的标题上加了「照抄暴君坑/主宰坑的框」：
下路河道 → 下路龙坑、上路河道 → 上路龙坑，**抄的是副本**，
之后改哪一个都不会牵动另一个。

### 播报四项加了说明

① 那张卡片顶上补了一段：带「播报·」的四项是屏幕正上方那条横幅里的分区
（谁击败的 / 被击败或被摧毁的是谁 / 哪种龙 / 助攻），
先框整条横幅，再在横幅内部分块；播报现在只是辅助验证，读不到不挡主判断。

### 测试补强（原来的写法漏得过去）

- `test_all_three_landmark_kinds_are_grouped_on_the_page` 写死了三类，
  改成遍历 `events_cv.LANDMARK_KINDS` —— 大招和召唤师技能加进后端时，
  旧写法不会有任何反应，而后端等着的框在页面上根本没有入口。
- 新增一组**真的把 `landmarkPlan()` 跑起来**的测试（用 node，没装就跳过）：
  字符串匹配挡得住「分组没了」，挡不住「分组在、循环少生成一半方块」。
  钉住 18/2/2/10/10/10、名字不重复、计划里的类型后端全都认
  （`save_landmarks()` 会**静默丢掉**不认识的 kind —— 用户会看到「保存成功」而其实没存）。
- 播报分区必须在**两种画面类型**的清单里都出现。
  第一版只搜整页文本，从其中一张清单里删掉一行照样通过 —— 变异测试当场抓到，已改。

### 验证到什么程度

- 变异测试 12 处（高地塔退回每方一座、删掉大招分组、少生成召唤师技能、
  少一个龙坑、从任一张清单删掉播报·助攻、龙坑抄反、抄成同一个数组引用、
  地标名重复、`toggleGroup` 顺手删数据、单人视角清单丢掉比赛计时……）
  **全部导致测试失败**。
- Playwright 实测：版本徽章 `v0.9.5 / 52 个地标框`；六组全部渲染、52 个方块；
  大招 / 召唤师技能 / 高地塔 / 龙坑都能标上并存进后端、刷新后还在；
  照抄按钮两个框都对且互不牵连；折叠让方块从 52 变 42、数据不变、刷新后仍折叠；
  从页面底部挑一个头像后画面自动回到视野；全程无 JS 报错。
- 全量 284 条测试通过。

### 刻意没做

- **没动 GPT 的检测逻辑**（遮挡状态机、时间窗口回溯、头像逐一匹配、时间偏移校正）。
  那些是这一轮他刚写的，我只补页面这一侧的入口和说明。
- **框仍不能拖动微调**，改一个框还是要重拖一次。

---

## 2026-09-09 · v0.9.5：视觉地标状态机、技能框与播报结构化标定

**实现**

- 每方高地塔由错误的1个通用框修正为对抗路/中路/发育路3个框；旧框自动迁移为中路框，已有标定不丢失
- 十名英雄分别增加大招绿点和召唤师技能框，并按蓝方左上/右侧、红方右上/左侧明确标注；扫描结果记录大招就绪状态变化
- 风暴龙王增加上下两个候选龙坑，20分钟后才启用并按同一只龙去重；主宰10分钟后正确解析为暗影主宰，不再错误写成风暴龙王
- 小地图从纯颜色中心点升级为“多尺度圆形候选 + 左右十个头像逐一外观/结构匹配”；真实帧发现低门槛仍会误认地图纹理后，改为高门槛拒绝输出，未核验阈值整体置信度封顶
- 推塔识别改为固定地标跨帧状态机：单帧变化记为遮挡、连续缺失生成候选、事后0.5秒窗口回溯；播报只提高/降低置信度，不作为必要条件
- 比赛时间前10秒全部跳过；播报增加总范围、击败者、被击败/被摧毁者、龙种类、助攻五层标定与结构化字段
- 事件扫描会用已有OCR/人工时钟标注估算“录像时间－游戏时间”偏移；长篇赛前节目不再被误当成游戏开局，回溯仍按原录像时间精确寻帧

**验证到什么程度**

- 合并同期标定页与评分模型改动后274个Python测试全过；新增遮挡恢复、推塔高帧率轨迹、播报非依赖校验、旧高地塔迁移、技能/风暴龙王类型、头像槽位匹配测试
- 新版临时服务浏览器实测：18座防御塔（含6座高地塔）、2个常规资源坑、2个风暴龙王候选框、10个头像、10个大招框、10个召唤师技能框均正常显示
- 用真实2026 KPL转播帧复核：低门槛会错误凑齐10点；收紧后仅保留2个高相似槽位，其余明确拒绝，未把结果提升为训练可信数据

**刻意没做**

- 不宣称轻量头像模板已经达到全员识别；当前优先消灭假点，漏检留待更多人工真值和跨帧跟踪解决
- 网络录像没有本地完整文件时先保存回溯窗口，不伪装成已经完成高帧率回溯；本地录像可立即执行
- 播报文字读不清或团战堆叠时保持未知，不强行编造击败者、目标或助攻

---

## 2026-09-09（第六轮）· 标定页：可隐藏的框、点得中的地标、看得懂的颜色旋钮

用户报了三条，全部是标定页的：框太多看不清地图、防御塔／资源坑／头像标不了、
小地图那几个数字看不懂。

### 1. 每个框都能单独隐藏（隐藏 ≠ 删除）

项目和地标每一行右边加了一个 👁。点它只是**不画这个框**，
`REGIONS` / `LANDMARKS` 里的坐标一个都不动。另外给了整体开关：
「显示全部框 / 隐藏全部框 / 只显示当前这个」，地标还能按组隐藏（塔 / 坑 / 头像）。
隐藏状态存 localStorage，刷新后还在；图上方常驻一行「已隐藏 N 个框（数据都在）」，
免得下次打开以为标定结果丢了。

选中一个被隐藏的目标会**自动帮它显示出来** —— 否则拖了框什么都看不见，像坏了。

### 2. 地标标不了：是下拉框吃掉了点击

原因不在后端。26 个地标以前挤在一个 `<select id="lmSel">` 里，
只靠 `change` 事件调用 `pickLm()`。**想标的如果正好是下拉框里当前显示的那一项，
再点它一次不会触发 change** —— `pickLm` 不跑，`LM_ACTIVE` 还是空的，
于是拖出来的框落到了上一次选中的普通项目上。表现就是「防御塔根本标不了」，
而服务器日志里一切正常。第一个选项是「蓝方对抗路1塔」，所以最常撞上的就是塔。

改成 26 个地标全部平铺成可点的方块，按防御塔 / 资源坑 / 英雄头像分三组，
每组显示「已标 n/m」。每个方块是独立的按钮，没有「点了等于没点」这种状态。

顺带补了两个同源的坑：图的正上方加了常驻的「正在标：XXX」；
什么都没选就拖框时，以前静悄悄什么都不发生，现在会明说「先在右边点一个」。

### 3. 「蓝 B-R」这种标签只有写代码的人看得懂

按 `minimap.classify_pixels()` 真正做的事重写了标签和说明：
「蓝点：蓝色至少多亮」「蓝点：蓝要比红多」「一团最少多少像素」，
每个输入框下面一句人话解释，外加一段可展开的原理说明，
写明判定一个像素算蓝方要**同时**满足三条，以及为什么光看「够不够蓝」会把白色和紫色算进来。
「试一次」之后按认出的人数给出调大还是调小的方向。

加了「恢复默认」按钮。默认值从后端来 —— `/api/status` 新增 `minimapDefaults`，
因为 `load_thresholds()` 返回的是「默认叠加已保存」，拿不回原始默认值，
前端另写一份数字两边一定会漂。

### 验证到什么程度

- 新增 `tests/test_calibrate_page.py`，15 条，锁住三条契约：
  地标必须有各自的点击入口（且不许退回下拉框）、隐藏代码里不许出现 `delete`、
  每个能填的数字必须配一句人话解释。
- **变异测试 8 处**：把下拉框放回去、让隐藏顺带删数据、删掉一句解释、
  让 overlay 忽略隐藏、后端不再给默认值、`pickLm` 不再清 `ACTIVE`、
  换回「蓝 B-R」、删掉资源坑分组 —— 8 处**全部**导致测试失败。
- Playwright 实测：26 个方块全部可点；先点普通项目再点第一个塔（就是原来失效的那条路径）
  能正确标上；坑和头像同样；隐藏一个 → 框从 3 个变 2 个而数据仍是 3 条；
  隐藏全部 → 0 个框、数据 3 条；刷新后隐藏状态还在；恢复默认把 90 填了回来；
  全程无 JS 报错。
- 全量 258 条测试通过。

### 刻意没做

- **没做框的拖动微调和缩放手柄。** 现在改一个框还是要重拖一次。
  用户报的是「看不清」和「点不中」，先解决这两个。
- **没碰标注页的小地图。** 那是另一套坐标逻辑，这轮不动。

---

## 2026-09-09（第五轮）· 样本类型筛选移到表头列上

用户反馈找不到上一轮做的勾选框。两个原因：

1. **他运行的是旧版本** —— 代码在推送的文件里（`console.html` 第 327 行），
   但本机没有 `git pull`
2. **位置本来就选错了** —— 我把它做成检索栏下面独立的一行，
   而用户直接指出应该做在**表头「样本类型」那一列**上。他是对的，
   表头就是大家会去找筛选的地方

### 改成了什么

「样本类型」这一列的表头本身变成一个可点的按钮，点开是一个下拉面板：

- 五种类型竖排，每种后面显示**有几场**
- 全选 / 全不选
- **表头上有徽章**（例如 `2/5`）：列表被筛过时一眼看得出来 ——
  否则「怎么少了几场」会被当成数据丢了
- 点面板外面或按 Esc 收起；**面板内的点击不冒泡**，
  否则勾一个就关一次，多选变成单选
- 记住上次勾选（localStorage），刷新后徽章和结果都还在

实测：默认 15 条 → 只留两类 8 条 → 徽章显示 `2/5` → 刷新后仍是 8 条。

---

## 2026-09-09（第四轮）· 比赛清单按样本类型多选筛选

用户要求：「样本类型」这一栏能勾选，只显示职业正式比赛、职业选手单排等。

### 做了什么

五种样本类型的复选框，**每种后面显示有几场** ——
光有勾选框只能筛，带上数量就能一眼看到「手上职业比赛有多少」。

- 和已有的文字检索**叠加**生效（勾了职业类 + 搜「暖阳」= 交集）
- 全选 / 全不选
- **记住上次的勾选**（localStorage）：只做职业比赛的人不该每次刷新都重勾一遍。
  读写都包了 try/catch —— 隐私模式下会抛异常，不能因此让页面挂掉。
  存的值会和当前类型列表求交集，避免旧数据把新增的类型永久藏起来
- 一个都没勾时明确提示「下面会是空的」，而不是让人以为列表坏了
- 「清空检索」会连带恢复全选

### 两个自己制造又修掉的问题

**① 每次勾选都重建整行 DOM。**
第一版在 `toggleSampleType` 里调 `renderSampleTypeFilter()` 重建整行复选框。
Playwright 报「Element is not attached to the DOM」才注意到 ——
这不只是测试问题：重建会丢焦点、点击手感发顿。
改成 `refreshSampleChips()` 只更新 class 和提示文字。

**② 替换锚点不唯一，改错了地方。**
把提示代码搬进新函数时，用的锚点在文件里出现了**两次**，
`.replace(old, new, 1)` 命中了错的那一处，结果
`refreshSampleChips` 变成了**自己调自己** —— 浏览器直接报
`Maximum call stack size exceeded`。

**教训：断言锚点「存在」是不够的，必须断言「唯一」。**
后来改成按行定位函数体重写，并在辅助函数里加了 `count == 1` 的断言。

---

## 2026-09-09（第三轮）· 修内置 HUD 坐标：小地图在左上不在左下

用户对着真实画面发现标注页的小地图裁出来是**赞助商横幅**。

### 我把 LOL 的习惯抄进了坐标

小地图的内置坐标被我放在**左下角** —— 那是 LOL 的位置（LOL 小地图在右下）。
**王者荣耀的小地图在左上角。**

顺带发现第二个同类错误：经济面板我放在顶部，
但用户早就说过「正式比赛会在**屏幕下方中间**较长时间展示经济面板」。

| 区域 | 原来 | 现在 |
| --- | --- | --- |
| 小地图 | 左下 | **左上** |
| 经济面板 | 上中 | **下中** |

**这个项目的核心文档就叫《为什么王者荣耀不能照抄LOL》，
而我在坐标上照抄了 LOL。** 已把方位钉进测试
（`BuiltinHudMatchesHonorOfKingsLayout`），并验证过：
把小地图改回左下，测试立刻挂。

只钉**方位**不钉数值 —— 具体的框仍然是猜的，
不同录像源的裁剪和分辨率都不一样，必须人工标定。

### 更重要的一条：裁剪成功 ≠ 裁对了地方

系统裁出赞助商横幅时**不会报任何错**，只有人能看出来。
而页面当时还写着「在这上面点是精确的」——
**框错了的时候，精确毫无意义。**

标注页现在区分两种状态：

- 标定过 → 「已标定，在这上面点是精确的」
- 没标定 → 黄色警告 +「**先看一眼：它是不是真的是小地图？**
  如果裁出来的是记分板、赞助商横幅或别的东西，
  在上面点出来的坐标全是错的」+ 标定页链接

测试 238 → 243 条。

---

## 2026-09-09（第二轮）· 补完分数模型：分数 → 胜率

上一轮的 `scoring.py` 缺了最后一步：**分数变不成胜率**。
`calibrate()` 只出一张表，而且没接进任何界面（CLI 和 UI 里 0 处引用）。

### 补的三样

**① Wilson 置信区间。**
「88% 胜率」来自 20 帧和来自 2000 帧，在表格里长得一模一样 ——
没有区间会让人对一个 20 样本支撑的数字产生不该有的信心。
用 Wilson 而不是正态近似：后者在比例接近 0/1 时会给出跑出 [0,1] 的区间。
实测：8 场时最宽区间 34.8%，150 场时 9.4%。

**② 保序回归（PAVA）+ 两种模式。**
某个分数档因为样本少，观测胜率可能比相邻的高分档还高 —— 那是噪音不是信号。
硬用会得到一条上下起伏的「胜率曲线」，而**胜率随分数差单调递增是这个模型
的基本假设**：要么承认假设，要么承认模型错了，不能两头都要。

映射带 `mode`：`fitted`（数据够，从校准表拟合）/ `assumed`（数据不够，
写死斜率的 logistic 顶着）。和 `model.py` 基线曲线同一套规矩 ——
`assumed` 模式下**每一次读数都带警告**。超出锚点范围的外推也明确标记。

**③ 接进 CLI 和分析页。**
`kplab scoring` 打印分项表、带区间的校准表、分数→胜率对照、分歧样本。
分析页新增分数模型卡片：分数差曲线 + 校准状态徽章，
**点拐点会显示那一刻的分项拆解**（哪一项贡献多少、缺了哪几项）。

### 变异测试：第五次栽在同一个地方

5 个变异里第一轮只抓到 4 个。漏掉的是「不做保序回归」——
因为我的合成数据本来就是单调的，删掉保序回归结果完全一样。
直接测 `_pool_adjacent_violators` 也没用：变异改的是**调用方**，不是函数本身。

**这是「契约要在契约所在的那一层测」的第五次。**
补了 `perverse_games()` 夹具（故意造出中间档赢得比高档还多的数据）
和 `IsotonicIsAppliedByTheCaller` 一组，其中第一条测试先验证
「夹具真的违反了单调」—— 否则后面那条是假阳性。重跑，5 个全抓到。

测试 221 → 238 条。

---

## 2026-09-09 · 交接给 GPT：文档校准 + 分工重写

Claude 这一轮工作告一段落，把状态整理清楚交给 GPT 继续。

### 项目最大的卡点已经被 GPT 打穿了

「一场真实比赛都没进过系统」这句话在交接文档里挂了很久，现在不成立：
1415 张 720P 真实关键帧、首场 53 帧人工真值、OCR 完成真实评估。

**其中 101/101 比 86% 重要得多** —— 机器说自己有把握时它是真的对，
说明置信度门槛这个设计经受住了真实数据的检验。
可信覆盖只有 64% 可以慢慢提，但「说有把握却是错的」补不回来。
所以**不许为了提高覆盖率放宽阈值**，已写进分工文档的「明确不要做的事」。

### 修掉的文档漂移

合并 GPT 的 6 个提交后审计，发现三处对不上：

| 问题 | 处理 |
| --- | --- |
| 模块表漏了 6 个模块 | GPT 重写表格时漏掉了 `minimap` `events_cv` `announce` `model` `scoring` `feasibility`，已补回 |
| 进度数字不一致 | 项目状态 32% vs 路线图 30%，已统一为 32% 并重算权重表 |
| 测试数三处三个值 | 221 / 204 / 58，已全部校准为 221 |

**文档和现实对不上比没有文档更糟** —— 接手的人会开始怀疑哪一边是真的。

### 分工文档重写

- **文件归属表**：原来只列 6 个文件，现在有 25 个模块。按主要作者重列，
  并写明「这只是避免同时改同一个文件，不是划地盘」
- **分工建议**：原来建议 GPT 做 V(s)，那是数据为零时的判断，已过时。
  重写为三条按优先级排的任务
- **给 GPT 的交接留言**：我这轮做了什么、三条我踩过的坑
  （契约要在契约那一层测——栽了四次、ffmpeg 静默失败、前端要用浏览器测）、
  以及那个一直没答案的问题（腾讯到底有没有开放对局接口）

### 交给 GPT 的三件事

1. 把 53 帧的流程扩到更多场次 —— 一场的 64% 覆盖率说明不了问题
2. 标定 26 个事件地标，让 `events_cv` 第一次在真实画面上跑
3. 等用户定义行为抽象 —— **在那之前 Q(s,a) 一步都动不了**

测试 204 → 221 条（含 GPT 新增的 HUD 模式、相对比分、并发写、裁剪测试）。

---

## 2026-09-09 · v0.9.4：比赛清单检索、分页和折叠

**实现**

- 比赛清单改为可折叠区域，默认展开；自动校验核心常量改为可折叠区域，默认收起
- 比赛清单新增重点对象和赛事/赛季双检索，支持中文、英文、空格差异和“KPL”插入等宽松匹配
- 新增10/20/50/100条每页数量、上一页/下一页、结果总数与当前页提示
- 清单新增“赛事/赛季”可见列；标签年份与录像标题不一致时显示警告，避免错误筛选被静默掩盖
- 页面直接解释“自动识别”和“准确率”的数据来源、限制与推荐使用顺序，按钮补充悬停说明
- 将当前4场DRG–TTG录像的错误赛季标签从KPL2025夏季赛纠正为KPL2026夏季赛，与录像标题及用户本次说明一致

**验证到什么程度**

- 真实数据浏览器验证：`TTG`得到4场、`暖阳`得到9场、`2026夏季赛`得到4场
- 13场数据默认分2页；切换每页20条后为1页；上一页、下一页、清空检索和两个折叠区域均已实测
- 221个Python测试全过；网页JavaScript语法和Git差异检查通过

**刻意没做**

- 检索和分页只改变当前页面显示，不删除、不移动、不重命名任何比赛数据
- “准确率”仍然只在有同帧人工标注时计算，不会为了展示数字而用机器结果自我验证

## 2026-09-09 · v0.9.3：网络录像实时进度与低负载分析流

**实现**

- 网络录像区新增持续可见的总进度条，显示总百分比、当前第几场、处理阶段、录像时间、证据帧数量、处理速度及本场/整批预计剩余时间
- 后台任务接口新增结构化阶段与进度字段；批次摘要保留当前场次的安全进度，服务重启后页面仍能判断任务位置
- 解析、读取媒体信息、流式抽帧、长录像侦察和完成阶段分别显示，不再用一个长时间不变的文本框冒充进度
- 正式抽帧优先采用约720P分析流，避免先解码1440P再缩小造成额外网络与本机算力消耗
- 运行中的批次不再显示误导性的“继续”按钮

**验证到什么程度**

- 当前4场KPL批次确认并非停止：第1场已完成579张，第2场正在处理，后2场排队；旧版1440P顺序解码约需数小时，是“看似卡住”的主要原因
- 221个Python测试全过；新增分析流选择、活动批次安全进度和恢复任务总进度测试；网页JavaScript语法、浏览器实时进度条和Python编译通过

**刻意没做**

- 不把网络速度和解码速度伪装成固定值；预计时间会按当前场的实时速度动态变化
- 不为了提速降低稀疏采样密度，仍保留前期8秒、中期5秒、后期3秒的证据帧计划

## 2026-09-09 · v0.9.2：首场人工真值、自动识别入口与可信度实测

**实现**

- 首场建立53帧人工真值，共106个己方/敌方比分字段；单人视角字段与蓝红阵营严格分离
- 控制台比赛清单新增“自动识别”和“准确率”按钮，OCR可后台批量运行、去重并替换旧版本结果
- 比分采用原图与灰度增强双路识别；两路不一致时强制降到训练可信阈值以下
- 新增连续性防线：比分倒退或短时异常跳变降级，计时器按整批中位偏移拒绝离群值
- 评估改为优先按同一张帧图配对，避免计时器读错后被静默跳过；分开报告普通准确率、读出率、可信准确率和可信覆盖
- gzip追加增加线程锁，修复并发标注可能损坏压缩观测流的问题

**真实验证结果**

- 53张人工帧中自动形成49条机器观测；3张计时漏读、1张异常计时被拒绝
- 共比较140个字段：普通读出率88%、普通完全一致率86%
- 达到训练可信阈值的101个字段为101/101完全一致，可信覆盖64%；覆盖仍不足，因此系统继续拒绝直接入模，只允许自动预填后人工复核
- 218个Python测试全过；Python编译、网页JavaScript语法和Git差异检查通过

**刻意没做**

- 没有因本场可信子集100%正确就宣称OCR整体可直接训练；低置信度草稿仍存在系统性误读
- 没有把己方/敌方比分映射成蓝方/红方，也没有用比分补造个人经济

## 2026-09-09 · v0.9.1：8场真实对局完成与单人视角 OCR 打通

**实现**

- 首条2小时36分33秒虎牙回放的8场完整对局全部完成高密度抽帧，产出1,415张720P源关键帧，约109 MB
- 高密度数据按对局分成8个独立样本；保畗157张低密度侦察帧，排除2段不完整观战/结算片段
- HUD 标定新增「赛事转播固定记分板」和「职业/顶尖选手单人视角」两种模式
- 单人视角的比分使用己方/敌方语义，未确认阵营映射时不冒充蓝方/红方；画面不常驻显示的双方总经济留为 unknown
- 修复对单张 JPG 做 HUD 裁剪时 `ffmpeg -ss 0` 返回成功却不产生文件的问题
- 本机安装 Tesseract，以真实帧验证计时识别：`07:44 → 464秒`、`08:38 → 518秒`
- 安全合并 Claude 同期的胜率基线页、拐点交互、地标/播报事件检测、点击小地图标注、位置精度分级、样本可行性分析和可校准评分模型；冲突已保留双方功能

**验证到什么程度**

- ✅ 8/8场任务全部 `DONE`，母任务为 `SEGMENT_EXTRACTION_COMPLETE`，0场失败
- ✅ 数据目录不含 MP4、MKV、TS、M3U8 或 WebM；不保存完整录像
- ✅ 合并后210个 Python 测试全过，Python编译、标定页 JavaScript 语法和 Git 差异检查通过

**刻意没做**

- 没有把己方/敌方比分自动写成蓝方/红方，因为单人视角的阵营映射尚未核对
- 小地图颜色阈值只是基于三张真实帧的候选值，尚未证明每个色块都是英雄，不标为已验证、不作训练真值
- 没有把界面上的假设胜率基线宣称为已训练模型；真实模型仍需要足够的人工真值和胜负标签

## 2026-09-09 · v0.9.0：首条虎牙真实长回放与断点恢复

**实现**

- 实际接入用户提交的2小时36分33秒虎牙录像，完成157张一分钟级侦察帧，仅占约6.5 MB，没有保存完整录像
- 长回放超过2000张智能基础帧时不再报网络错误，自动切换为低清稀疏侦察并标记为待分场
- HLS侦察直接读取播放清单并定位对应3~5秒媒体小段，解决虎牙整条m3u8在约15分钟后无法远距离跳转的问题
- 侦察自动选择最低可用清晰度，三个时间点并行读取；正式分场抽帧使用更高画质
- 服务重启自动恢复PENDING/RUNNING批次；登录态读取超时后自动改用公开页面，失败批次可从控制台直接重试
- 已从首条长回放划出8场可用完整对局，排除2段不完整观战/结算片段，并建立8个独立高密度抽帧任务
- 分段边界明确标为0.8置信度的视觉审阅结果，不冒充人工训练真值

**验证到什么程度**

- ✅ 首条真实虎牙回放已完成全时长侦察，157/157个一分钟时间点成功，磁盘占用约6.5 MB
- ✅ 8个分场档案已建立并进入前期8秒、中期5秒、后期约3~5秒的抽帧队列
- ✅ 96个Python测试全过；新增重启恢复、登录态降级、长回放侦察、清晰度分流、HLS小段定位和分场建档测试

**刻意没做**

- 没有把一分钟侦察帧当成战术分析数据；它只负责定位场次，高密度帧另行生成
- 没有把短暂观战片段或没有完整边界的内容塞进训练样本
- 尚未完成HUD人工标定和真实OCR准确率验证，因此还没有宣称生成可信结构化比赛状态

## 2026-09-09 · v0.8.0：S44官方说明优先级与冲突裁决

**实现**

- 将用户再次提供并明确声明为官方说明的26条S44赛季规则提升为最高资料优先级
- 21条数值明确的官方规则继续参与计算；5条官方原文有歧义或缺数值，仍只保存不猜值
- 所有核心常量新增“官方说明（用户提供）”来源标记，同时保留未附原始公告链接、未独立外部核验的事实
- 4条非官方经济资料冲突由“待处理冲突”改为“已按官方说明停用”
- 控制台同步显示官方来源、资料优先级、停用原因和仍存在的原文歧义

**冲突裁决**

- 暴君重生使用官方说明210秒，停用非官方经济文档的240秒
- 前4分钟中路使用官方说明1近战+2远程，停用非官方文档的2近战+2远程
- 二塔视野之灵使用官方说明20秒重生，停用非官方文档的120秒
- 红隼保留官方原文“25～20”，停用非官方固定20金币；因条件不明仍不计算
- 边路兵线“180分钟”和防御塔护盾错字仍按官方原文保留，不擅自改成推测值

**验证到什么程度**

- ✅ 88个Python测试全过，新增官方/非官方优先级与官方歧义不猜值测试
- ✅ Python编译、四个网页JavaScript语法与Git差异检查通过

**刻意没做**

- 没有把“用户声明为官方”伪装成已打开腾讯原始公告完成独立核验
- 没有根据常识把“180分钟”自动改为18分钟，也没有把“25～20”猜成固定值

## 2026-09-09 · v0.7.0：S44个人通识参考与版本资料隔离

**实现**

- 完整审阅用户提供的4页《S44王者荣耀通识》及24张嵌入截图，整理20条结构化参考记录
- 新增 `knowledge_s44.py`，覆盖基础术语、战术术语、分路判断、角色判断与版本资料
- 7条明确术语允许用于人工标注名称归一化，但返回值始终保留个人总结来源和0.4置信度
- 控制台新增通识参考库，逐条显示来源页、用途状态和风险说明
- 英雄热度榜、装备商店、召唤师技能与惩击数值作为版本快照保存，全部禁止进入模型或硬性校验

**矛盾与过度概括处理**

- “中路必须是法师”改为常见选择，实际分路须按线路、资源与支援路线判断
- “游走必须购买学识宝石”改为辅助装备只是多证据之一
- “打野必须携带惩击和打野装备”改为强提示，保留特殊阵容的可能性
- 屏幕左右不能直接决定对抗路/发育路，避免观战镜像和地图版本造成误标
- 2026-09-07英雄热度榜截图标有“算法测试中”，不录入固定英雄分路或强度结论

**验证到什么程度**

- ✅ DOCX共4页全部渲染并逐页检查，24张原始嵌入截图全部使用本机文字识别读取
- ✅ 86个Python测试全过；新增来源隔离、绝对说法降级、版本快照隔离与术语归一化测试
- ✅ Python编译、四个网页JavaScript语法与Git差异检查通过

**刻意没做**

- 没有把个人经验包装成官方规则，也没有把英雄榜、装备价格或惩击数值当成当前补丁事实
- 没有仅凭英雄职业或一件装备自动判定实际分路

## 2026-09-09 · v0.6.0：S44非官方经济机制参考库

**实现**

- 完整审阅用户提供的7页《S44王者经济机制》，从45张嵌入截图中整理32条结构化经济规则
- 新增 `economy_s44.py`，覆盖英雄/装备、开局与自然经济、兵线、野怪、龙、防御塔、人头、辅助装和召唤物经济
- 6条内部完整公式允许进行带来源、0.55默认置信度的参考估算；全部32条都禁止作为硬性合法性校验或训练真值
- 控制台新增经济机制参考库，逐条显示来源页、用途状态和注意事项
- 新增自然金币、多人吃线/吃野、防御塔经济三个安全估算接口，返回结果始终携带非官方来源标记

**冲突处理**

- 文档称普通暴君重生240秒，现有用户确认值为210秒：继续使用210秒
- 文档称前4分钟中路2近战+2远程，现有用户确认值为1近战+2远程：继续使用现有规则
- 文档称二塔视野之灵120秒刷新，现有用户确认值为20秒：继续使用20秒
- 文档称红隼队友固定20金币，现有资料为“25～20”：保留为冲突候选，不参与计算
- 文档出现15至25级经济表：判定为训练营/指挥官模式范围，不进入标准S44比赛模型

**验证到什么程度**

- ✅ DOCX共7页全部渲染并逐页检查，45张原始嵌入截图全部使用本机文字识别读取
- ✅ 81个Python测试全过；新增来源隔离、冲突集合、自然金币、多人数分配和防御塔经济测试
- ✅ Python编译、四个网页JavaScript语法与Git差异检查通过

**刻意没做**

- 没有把业余实测资料冒充官方数据，也没有覆盖用户此前明确提供的S44规则
- 没有对悬赏积分、最低经济突变、辅助装拾取上限等条件不完整内容强行补公式

---

## 2026-09-08（第八轮·Claude）· 分数模型 + 校准：让一组假设变得可被证伪

用户问：把英雄强度、血量、技能CD、兵线、经济、装备都量化成分数，
由分数推出分支胜率 —— 这和因果推断是一类模型吗？哪个更好？

**不是一类，而且失败方式正好相反，所以应该合用。**

### 两者的定位

分数模型本质是**手工设计的 V(s)**（状态评估），
因果推断回答的是 Q(s,a)（换个打法会怎样）。

- 分数模型的死穴：**只反映你已有的认知**。
  权重说开龙值 5 分，模型就说开龙好 —— 那是假设的回声不是证据。
  而且它**自洽**，错误以系统性偏差存在，不体现在任何置信区间上。
- 因果推断的死穴：**重叠性**。没人做过的选择答不了。

**但分数模型能推演数据里从没发生过的动作** —— 那正是重叠性的死穴。
反过来因果推断能推翻先验，那是分数模型做不到的。所以互补。

### 新增 `scoring.py`，核心是 `calibrate()`

**它让分数模型变成可证伪的**：分数差 +20 的局面，实际胜率真的更高吗？
不是就说明权重错了 —— 不管纸面上多有道理。
一个不能被证伪的分数模型和占卜没有区别。

合成数据上验证：正常权重 8% → 88% 单调；
**把经济权重定成负的，校准立刻报不单调并指出「某一项方向定反了」**。

配套：`disagreements()` 找「分数说稳赢、结果输了」的局面
（一致的地方什么也学不到）；`fit_weights()` 带先验的拟合，
数据少时贴近你的权重、数据多时能拉走。

### 必须先说的现实：一半分项拿不到

用户列的分项里按权重算约一半**现在拿不到**：

| 拿得到 | 拿不到 |
| --- | --- |
| 经济、中立资源、推塔、等级、存活人数 | **血量、技能CD**、英雄强度、兵线、关键装备 |

血量和技能CD**不是识别难度问题，是信息本身不存在** ——
转播画面上没有任何地方稳定显示十个人的血量和 CD。
用户举的例子（「减去技能CD不健康」）正好落在拿不到的那一半。
`score_frame()` 返回 `ceiling` 说明这一点：现在的分数反映的是
「资源和进度」，不是「当下战斗力」。

### 两个自己写出来又改掉的设计错误

**① coverage 的分母用错，模型一帧都算不出来。**
第一版拿「已读到的权重 ÷ 全部权重」当覆盖率。
但血量、技能CD 这些占了约一半权重且**永远拿不到**，
所以覆盖率永远到不了 60% 的阈值，`comparable` 恒为 False。
跑校准时零行输出才发现。已拆成两个数：
`coverage`（能拿到的部分里读到了多少）和 `ceiling`（有多少是永远拿得到的）。

**② `fit_weights` 要求所有分项齐全，一个缺失废掉全部 1800 帧。**
改成按出现率筛（≥50%），但被排除的分项必须明说 ——
**它们的效应会被留下的项吸收**，剩下的权重因此是偏的。

### 用户的例子里还藏着第三样东西

「红方可以凭更高战斗力通过打架得分」是**状态转移模型**。
评估函数 + 转移模型 = 前向推演（象棋引擎的结构），
那是另一套假设，需要单独验证。已写进 `compare_paradigms()`。

变异测试 5 个全抓到（含「校准不检查单调性」和「拿不到的分项当 0」）。
测试 183 → 204 条。

---

## 2026-09-08（第七轮·Claude）· 位置精度分级 + 样本可行性；修一个静默失败的 ffmpeg bug

用户提了两个问题，都问到了要害。

### 一、位置标注：我上一轮只改对了一半

用户的原话：「如果需要在你的小地图上精确定位录像中头像在小地图上的位置，
则完全不行，因为无法预估录像中小地图在你的小地图上的坐标，
但可以做到在录像的小地图上精确定位。」

**对的。** 手画的示意图和真实小地图不是等比的，没法把录像上的一点
对应到示意图的同一点。而原来两条路都写 `manual` + 0.98 置信度 ——
精度差一个量级却一视同仁。

已改：标注页**优先显示录像里的真实小地图**（裁出来放大到 520px），
在上面点是精确的（0.98）；没抽帧/没标定时才退回示意图，
置信度 0.55（低于可信线 0.75，进不了模型特征），
并在界面上写明「只能得到大概位置」。

### 二、样本量与因果可行性

新增 `feasibility.py` + `kplab feasibility`。三条结论：

**① 个人视角录像不是废了，是分层用。**
看不到经济面板 → 做不了经济曲线；但 `events_cv` 那条路不需要经济面板，
推塔/打龙/击杀看图标就行。缺经济特征的帧本来就会被自动跳过，不用写新代码。

**② 卡住因果推断的不是「多少场」，是重叠性。**
职业选手在某些局面下从来不做某个选择 → 那个格子永远是空的 →
再加十万场也填不上。由此得出一个反直觉的结论：

> 只用职业比赛，会让测量变好，但让**因果识别变差**。
> 路人局会做职业选手不会做的选择，那恰恰是空格子的填充物。

**③ 跨赛季要分清「漂移」和「断裂」。**
降权能处理版本微调，处理不了「暴君收益改了」这种结构性变化 ——
降权只是让错误的信号小声一点，方向仍然错。
`season_stability()` 按赛季分别拟合再比系数来判断。

### 一个我自己算错并修正的数

第一版把 Q(s,a) 的需求按「每场 20 帧」算，得出「120 场就够，只比 V(s) 贵一倍」。
**那是错的。** 像「入侵野区」这种动作一场只发生三五次，
样本单位是**动作实例**不是帧。改正后：

| 一场能观察到几次 | 需要 |
| ---: | ---: |
| 1 次 | 2400 场 |
| 4 次 | 600 场 |
| 8 次 | 300 场 |

这种低估最危险 —— 它会让人以为「几百场就能做因果推断」。

### 三、一个静默失败的真 bug（影响面比发现它的地方大）

做「真实小地图放大」时接口报 500。查下去发现：
**`ffmpeg -ss 0 -i 静态图片` 什么都不输出，但退出码是 0、stderr 是空的。**

影响的不只是新功能：`ocr.read_regions()` 也是拿帧图 + `0.0` 调
`crop_region` 的 —— 也就是说**对帧图跑 OCR 一直拿不到裁剪结果**，
而且完全静默。已改为 `at_sec <= 0` 时不加 `-ss`，
并把「ffmpeg 没报错但也没产出文件」写进错误信息（原来抛出来是空的）。

### 还有一个我自己的操作失误

改标注页时用了 `.replace()` 却没加 assert，
结果 `useRealMinimap` 的**定义**没插进去，只插了调用点。
浏览器里报 `useRealMinimap is not defined`。
**光跑后端测试永远发现不了** —— 这是第二次靠 Playwright 抓到前端问题。

测试 164 → 183 条。

---

## 2026-09-08（第六轮·Claude）· 用播报补归属：粗扫定区间 → 区间内精抽 → 判队伍

**进度：26% → 30%**

上一轮明确写着「图标消失说不出是谁拿的，只能人工补」。
用户给了解法：**推塔、击杀、开龙都会在屏幕中间上方弹一条播报，而且停留几秒。**

从图标消失那一帧往前找到「图标还在」的那一帧，事件就被夹在这两帧之间；
**回到这个区间里精抽帧**，就能找到那条播报，从而读出归属。

### 为什么这个方法可行

关键是**只精抽区间**。整场 20 分钟按 1.5 秒抽是 800 帧，
但每个事件只需要在 60 秒窗口里抽约 41 帧，一场十几个事件也就几百帧。
`plan_cost()` 会把这笔账直接打出来。

### 只判队伍，不读文字

横幅上有「谁击杀了谁」，但读文字又回到 OCR 那条不可靠的路上。
这里只看横幅整体偏蓝还是偏红 —— 判队伍就够了，
State 里的中立资源计数器和推塔数本来就是按队伍记的。
置信度封顶 0.75：颜色本身不是铁证。

### 三种仍然是「不知道」的情况

1. 区间里没找到横幅（框歪了 / 精抽间隔跨过去了 / UI 版本不同）
2. 横幅颜色两边都不明显（差值 < 12 就判不出）
3. **一个区间里挤了多个事件** —— 团战里三个人头加一条龙共用一个区间，
   那条播报属于哪一个根本判断不了。置信度减半到 0.375，
   **正好过不了 0.5 的写入门槛**，所以这类猜测不会进数据

### 合成录像验证

造了一段 600 秒的假录像：暴君 t=180 消失（t=170~176 有蓝色播报）、
塔 t=300 消失（t=286~292 有红色播报）。
粗扫先得出「暴君归属不明」，`--refine` 之后正确补成蓝方，置信度 0.75。
只精抽了 41 帧。

### 变异测试：先漏了 2 个，补测试后全抓到

| 变异 | 第一次 | 补测试后 |
| --- | --- | --- |
| 团战区间不减半 | ❌ 漏 | ✅ |
| 颜色分不清默认蓝方 | ✅ | ✅ |
| 区间往前找错一格 | ✅ | ✅ |
| 置信度不封顶 | ✅ | ✅ |
| 没标定也照给归属 | ❌ 漏 | ✅ |

漏掉的两个原因**和前几次一模一样**：测试只碰到了 `refine()` 的守卫路径，
没碰到主流程 —— 「减半」那条是自己把数字乘 0.5 再断言，根本没调用被测代码；
「没标定要拒绝」那条顺带用了个不存在的录像，于是删掉标定检查后被
「找不到录像」兜住了。

补了一组 `RefineOnARealVideo`：用 ffmpeg 现造一段 20 秒的小录像，
真正跑通 `refine()`，直接对比「一个事件」和「三个事件」两种情况的置信度。
这是**第四次**栽在「契约要在契约所在的那一层测」上了。

### 顺带修的两个东西

- **守卫顺序**：`refine()` 原来先检查录像存不存在，
  导致「所有事件本来就有归属、根本不需要精扫」的场景也会失败一次。
  改成先看有没有活要干
- **质量报告过时**：粗扫时记的「N 个资源判断不出归属」在补完之后仍然挂着，
  于是报告上面写「蓝方拿到暴君」、下面写「判断不出归属」，自相矛盾。
  新增 `refresh_quality()` 在补完后重算。
  **报告和数据对不上比没有报告更糟** —— 看的人会开始怀疑哪边是真的

测试 140 → 162 条。

---

## 2026-09-08（第五轮·Claude）· 看图标变化推事件（用户提的思路，比 OCR 好一个数量级）

**进度：21% → 26%**

用户指出：不用去 OCR 记分板上的数字，直接看**图标的出现与消失**就行 ——
塔被拆了小地图上图标就没了，龙被打了坑的图标就没了，
英雄死了头像栏那个头像会变灰，右上角比分还能佐证。

**这个思路比原来的 OCR 路线好得多。**「某个固定位置的图标还在不在」
比「这串像素是几」容易判断一个数量级，而且不受字体、缩放、压缩噪点影响。

新增 `events_cv.py`。

### 两条守住的规矩

**① 塔是单向的，中立资源是周期的 —— 两条状态机，刻意不合并。**

塔的图标消失了就永远不回来。暴君会刷新，图标消失又出现完全正常，
而且「消失 → 再出现」恰恰是一次完整的击杀 + 刷新周期。

照抄塔的逻辑去处理暴君会得到「暴君被拆了 6 座」——
换个地方重犯 LOL 那个「先锋算成大龙」的错。
所以 `TowerTracker` 和 `ObjectiveTracker` 是两个类。
另外：塔判为摧毁后图标又出现 = 判定错了（塔不会重建），记为矛盾并把整场判为不可信；
资源两次击杀间隔短于刷新时间 = 图标抖动，丢弃并记录。

**② 两个独立信号对上了才算击杀。**

头像变灰和比分加一来自画面上不同的两块区域，出错原因也不同：

| 情况 | 处理 |
| --- | --- |
| 两个都动 | 高置信度击杀，且知道是谁死的 |
| 只有比分动 | 确实死了人但不知道是谁，只记团队数 |
| **只有头像变灰** | **可疑**（特效遮挡也会变灰），不当击杀 |

还加了一条：比分加的必须是死者的**对面**，同队加分对不上不算数。

### 明确答不了的：资源是谁拿的

图标消失只说明资源死了，**说不出是谁打的**。
附近有比分变化最多算「倾向」（置信度 0.35）—— 打赢团战和拿到资源不是一回事。
归属不明的事件**不写进观测**，宁可漏不可错。

### 合成帧验证

造了一串已知真值的帧（塔 t=300 消失、暴君 t=180 消失 t=420 刷新、
2 号位 t=240 变灰 t=360 复活），三项全部命中，
且暴君刷新**没有**被误判成第二次击杀。

### 变异测试 6 个全抓到

资源用塔的逻辑、塔图标回来不记矛盾、只凭头像变灰算击杀、
归属不明硬塞给蓝方、低置信度降级写入、暴君和黑暗暴君记成同一事件。

### 浏览器实测抓到的 bug

`calibrate.html` 里用了 `esc()` 但那个页面根本没定义它（只有 analysis.html 有），
直接抛 ReferenceError 把整段渲染打断。表现是
**「保存说成功了，列表却是空的」** —— 后端一切正常，光看服务端日志永远查不出来。

### 其他

- 标定页新增地标标定：26 个地标（12 座塔 + 2 座高地 + 2 个资源坑 + 10 个头像），
  框好后可以直接点「用它跑一遍事件检测」看结果
- 新增 `kplab events <编号>`，加 `--write` 才真的写进观测
- 顺带发现 GPT 已经把 `stormDragonFromSec` 从我猜的 900 改成了查证过的 1200
  （标为 `_confirmed`）。我的代码从共享的 rules 表读，自动跟上了

测试 117 → 140 条。

---

## 2026-09-08（第四轮·Claude）· 「位置」栏改成点地图，并修一个坐标系错配的 bug

用户问「位置这一栏怎么填」。看了一眼发现两个问题，一个是易用性，一个是真 bug。

### 真 bug：两套坐标系对不上

标注页原来是：在比赛画面上点小地图里的英雄，存下**相对整幅画面**的比例。
但下游全都按**相对小地图**解释 —— 核对页的小地图、`minimap.py` 的输出都是。

后果：画面里小地图在左下角，点中间的英雄得到 x≈0.05，
当成地图坐标就跑到最左边去了。**存进去的每一个位置都是错的，
而且错得很安静** —— 数字在 0~1 之间，校验拦不住，页面上也画得出来。

已改为用标定过的小地图区域做换算；没标定过就明确拒绝并说明，不再默默存错值。

### 易用性：不该让人打小数

原来是两个数字框，要求填 0~1 的小数。对一个不写代码的人来说这是最糟的交互 ——
没人能一边看录像一边估算「他大概在 0.42, 0.61」。

改成：点行选人 → 点小地图 → 自动跳下一个人。
并把坐标翻译成人话（「中路 / 河道一带」「蓝方半区 · 上半区」），
让人当场就能看出点没点错。位置仍然不是必填的。

### 顺带画了张王者峡谷示意图，画错两次

- 第一版：河道和中路**平行** —— 河道本该横穿三路
- 第二版：路的线段叠成一团，看不出是三条路
- 第三版：三条折线各自从蓝方基地通到红方基地，河道垂直横穿，主宰/暴君落在河道上

明确标注是**示意图不是等比复刻**，点大概位置就够了。

### 测试期间的一个假警报

Playwright 报「点了没反应」，查了一圈发现是我把视口设成 640px 高，
而地图在 y=1044 —— 点击落在视口外。`elementFromPoint` 返回 null 才看出来。
不是代码问题。加了 `scroll_into_view_if_needed` 后一切正常。

---

## 2026-09-08（第三轮·Claude）· 分析界面：胜率曲线 + 拐点 + 明确留空的分支胜率

**进度：16% → 21%**

用户问：有没有「上传训练赛录像 → 出胜率曲线 → 拐点加粗圆点 → 点击看分支胜率和分析」
的界面。答案当时是没有。这一轮把这条链路做出来了，
**但中间有一段是假的，而且必须一直是明说的**。

### 新增 `model.py`

- 特征抽取：**只用 `schema.trusted()` 的字段**，绝不读 `meta.blueWin`，绝不读后面的帧
- 纯标准库的逻辑回归（梯度下降），不引入 sklearn
- 中立资源五种分开加权（暴君 1.0 / 黑暗暴君 2.0 / 主宰 1.5 / 风暴龙王 3.0 / 先知主宰 2.5）——
  权重不同才算真的没合并，同权重等于换个地方犯 LOL 那个 bug
- **训练拒绝条件**：<30 场、<400 帧、只有赢的（或只有输的）、可信字段 <50%，
  任一条不满足直接抛 `TrainingRefused`。抄的是 LOL 项目 `train.py` 少数做对的地方

### 关于那条曲线是假的

没有真实数据就训不出模型。两条路：什么都不显示，或者先用一条
**公开承认是假设**的曲线把链路跑通。选后者，理由和 LOL 路线图 AI2 一样。

防线是硬的，七条，每条都有测试钉住：

1. 系数写在 `BASELINE_PRIORS`，每条附「凭什么这么定」
2. 曲线带 `kind` 字段：`baseline` / `trained`
3. 基线**不给准确率**（`accuracy: None`）
4. 页面顶部大号琥珀色警告横幅
5. 基线的线画成**虚线**，训练出来的才画实线
6. `train()` 数据不够直接拒绝，**不会悄悄退回基线**
7. `_load_model()` 只认 `kind == "trained"` 的文件

### 分支胜率：**故意留空**

点开拐点，「如果当时换个打法会怎样」那一块是一个明确的
「未实现」区块，写清楚了为什么：

那是 Q(s,a)，必须是**因果**估计。简单拟合「做了 A 的人赢了多少」
会学到「优势方才有余裕做 A」—— LOL 项目里 GPT 那版就是这么干的，
用户当时明确警告过要避免。

**编一个「如果当时不开团，胜率 61%」出来，看起来正好是用户想要的东西，
而且没有任何一步会报错。这是整个项目最容易骗到人的地方。**

### 新增 `/analysis` 页面

胜率曲线（50% 以上填蓝、以下填红）、拐点加粗圆点可点击、
悬停十字线 + tooltip、拐点清单按波动排序、点开看「这一段发生了什么」、
表格视图。配色跑过 dataviz 校验器（深色底 #171b22 上蓝红 CVD ΔE 26.7，全部 PASS）。

### 用浏览器实测抓到三个 UI bug

光看代码看不出来，是 Playwright 点不动才发现的：

| 问题 | 原因 |
| --- | --- |
| 拐点圆点点不动 | 透明命中圈画在圆点之后，盖住了它 → 改成两趟画 |
| 还是点不动 | 十字线跟着光标走且在最上层 → 加 `pointer-events="none"` |
| 拐点没有 tooltip | 只给命中圈绑了悬停，圆点盖在上面 → 悬停逻辑统一成一份 |

第三个尤其值得记：**最值得看的那几个点恰好是没有 tooltip 的那几个。**

### 变异测试：7 个变异全部被抓到

| 故意做的坏事 | 抓到 |
| --- | --- |
| 基线曲线谎称是模型输出 | ✅ |
| 基线也给个准确率 0.87 | ✅ |
| 去掉训练拒绝条件 | ✅ |
| 只有赢的比赛也不拦 | ✅ |
| **编一个分支胜率出来** | ✅ |
| 低置信度字段也当特征 | ✅ |
| 风暴龙王和暴君同权重 | ✅ |

期间还踩了个坑：变异把 `3.0` 改成 `1.0`，字节数相同，
`__pycache__` 没失效，导致「恢复后」仍然失败。清缓存后正常。

测试 95 → 116 条。另修正一条我自己写错期望的测试：
以为「这一帧没读到就必须跳过」，实际沿用一帧是设计好的行为
（0.98×0.85=0.83 仍在可信线以上）。已改为钉住真实行为。

---

## 2026-09-08 · v0.5.0：网页录像来源与流式分析

**实现**

- 新增独立 `sources.py`，将来源识别、平台 Resolver、媒体流、任务批次与 CV/抽帧解耦
- 支持本地录像、直接 HTTP 视频、HLS/m3u8、Bilibili、虎牙、斗鱼和通用网页
- 网页平台统一优先通过 yt-dlp 解析，并按平台注册为可替换 Resolver；未硬编码平台抓取算法
- 控制台新增多网址粘贴、TXT/CSV导入、解析预览、分P展开、重复提示和浏览器登录状态选择
- 远程批次逐项排队，单项失败不阻断后续任务；网络或媒体地址失效最多重新解析3次
- `originUrl` 长期保存；带签名的 `sourceUrl`、Cookie和请求头只在内存中使用，不写日志或任务文件
- ffmpeg 对远程来源只连续解码一次，经 Frame Dispatcher 分发；不生成完整录像副本
- 只保存阶段加密的稀疏证据帧；临时缓存限制为2GB/24小时并自动清理
- 增加内网地址、凭证网址、DRM/登录/权限错误、请求头注入和临时地址泄露保护

**验证到什么程度**

- ✅ 75个Python测试全过，新增来源检测、播放列表、失败隔离、登录态内存传递、隐私、缓存、队列和单次解码守卫
- ✅ 12秒HTTP录像实测：流式读取并保存6张证据帧，目录中没有完整MP4副本
- ✅ 8秒HLS/m3u8录像实测：流式读取并保存4张证据帧，目录中没有完整录像副本
- ✅ 本地智能流式抽帧实测：12秒录像按0秒、8秒生成2帧
- ✅ yt-dlp 2026.8.19_1 与 ffmpeg 已安装，自检可以识别
- ⚠️ 匿名访问一个公开B站页面时平台返回HTTP 412；系统正确归类为访问受限，不会循环重试或伪装成功

**刻意没做**

- 不绕过登录、付费、地区限制或DRM
- 不永久保存临时CDN地址、Cookie或完整远程录像
- 当前保存的是稀疏证据帧；小地图、OCR等CV模块尚未全部接入连续流调度器

---

## 2026-09-08 · v0.4.0：S44赛季规则库

**实现**

- 将用户提供的S44规则拆成26条结构化记录，覆盖0/4/10/20分钟阶段、
  中立资源、兵线、防御塔、原初法阵、视野之灵、红蓝BUFF和红隼
- 21条数值明确的规则标为“用户已核对”并允许计算；5条有歧义或缺具体数值的
  规则仅保存，不参与自动计算
- 暴君与主宰首刷统一修正为4分钟，暗影暴君/暗影主宰为10分钟，风暴龙王为20分钟
- 校验器新增红蓝BUFF、暗影暴君、暗影主宰、风暴龙王的最早击杀时间检查
- “黑暗暴君/先知主宰”界面名称更新为S44使用的“暗影暴君/暗影主宰”，
  内部旧字段继续保留，避免破坏已有数据
- 控制台新增S44分组规则页面，新导入录像的游戏版本默认填写S44

**保留的不确定项**

- 边路兵线速度原文写“180分钟后达到495”，疑似18分钟，暂不参与计算
- 一塔已损生命护盾的触发句存在错字，触发次数和持续时间不明确
- 暗影暴君加强版君临、20分钟后兵线成长缺少具体数值
- 红隼团队每人金币“25～20”的变化方向和条件不明确
- “用户已核对”不代表已经用官方公告二次验证，来源状态已明确保留

**验证到什么程度**

- ✅ 与小地图识别改动合并后，60个Python测试全过
- ✅ 新增测试确认S44核心时间、歧义隔离和不可能的提前击杀会被拦截
- ✅ 控制台JavaScript语法检查通过

**刻意没做**

- 没有猜测上述5条不完整规则的缺失值
- 没有把规则说明误称为已经训练好的行为识别模型

---

## 2026-09-08（第二轮·Claude）· 小地图识别 + HUD 标定页

**进度：9% → 13%**

### 补上了路线图里标的最大缺口的一半

`minimap.py`：认出小地图上有几个蓝色标记、几个红色标记、各在哪。

**不装任何 Python 包怎么处理图像：** 标准库没有 JPEG 解码器，而这个项目
不许用 pip。解法是让 ffmpeg 把画面区域直接吐成原始 RGB 字节流
（`-pix_fmt rgb24 -f rawvideo -`），Python 那边就只是读 bytes，
外加一个纯手写的四邻域连通域标记。ffmpeg 本来抽帧就要用，不引入新东西。

合成画面上验证：10 个标记全部找到，位置误差 <0.012（容差是 0.05）。

**明确没做：认不出是哪个英雄。** 区分英雄要头像模板库、要处理遮挡和缩放，
是另一个量级的工作。所以 `to_observation_players()` 按位置排序硬塞进槽位时，
置信度只给 0.15 —— 位置本身可能对，但「这是几号位」基本是猜的。
**「入侵野区值不值」仍然答不了**，那需要知道敌方打野这个具体的人在哪。

### 新增 `cv` 来源，和 `ocr` 分开

记分板 OCR 错了通常是数字认错（8 读成 9），小地图 CV 错了通常是
「哪个点是几号位」认错 —— 两类错误的排查方式完全不同，
混成一个来源会让以后查问题的人无从下手。

### `/calibrate` 标定页

拖框标定 HUD 区域 + 小地图颜色阈值实时调试。
识别出来的人会直接画在画面上，超过每方 5 人当场判为不可用。

### 一个测试时发现的真 bug

阈值调松后，整个小地图底色连成一个巨大的蓝色区域，它因为超过尺寸上限
被**静默丢弃**，于是「蓝方 0 个标记」看起来像正常的「没找到」，
整帧被判为可用。

一个超大团块被丢弃，恰恰是阈值错得离谱的最强信号 —— 把它当成
「什么都没找到」是把最有价值的诊断信息扔了。已改为报出来并判为致命。
同时补了两条守卫：染色面积超过 35%、以及「一方全无而另一方好几个」。

### 变异测试又抓到同一类盲点（第二次了）

新写的小地图测试全过之后，照例做变异测试：

| 故意删掉的守卫 | 抓到了吗 |
| --- | --- |
| 「每方最多 5 人」 | ❌ 全过 |
| 「超大团块要报警」 | ❌ 全过 |
| 「不可用时不产出位置」 | ✅ 抓到 |
| 「cv 置信度要压低」 | ✅ 抓到 |

前两条没抓到的原因和立项时 `schema.derive()` 那次**一模一样**：
夹具同时触发了别的守卫，于是别的守卫兜住了，这道守卫其实没被测到。

**有别的守卫兜着，不等于这道守卫被测到了。**
已补 `EachGuardPinnedOnItsOwn` 一组：直接调 `assess()`，每次只让一个条件成立，
逐条钉死。重跑四个变异，全部抓到。

### 新增 `分工与协作.md`

两个 AI 同时改一个仓库会互相覆盖 —— 这一轮就真的撞上了（见下一条 GPT 的提交）。
约定了分支、文件归属，以及一块给 GPT 的留言板。

---

## 2026-09-08 · v0.3.0：样本分层、阶段加密采样与批量录像处理

**问题**

- 职业正式比赛数量有限，不能把它当成唯一录像来源
- 职业比赛、职业选手排位和其他高端局分布不同，混在一起会污染战队打法分析
- 固定30秒抽帧会漏掉完整团战、反野、卡线和埋伏过程
- 每段录像先手工建一场记录，无法规模化导入

**实现**

- 新增职业正式比赛、职业选手单排、其他顶尖选手、训练赛、其他录像五层标签
- 每场保留选手、战队、游戏版本、分段、赛事、自定义标签和可调训练权重
- 职业正式比赛带独立稳定标记，可供后续提高权重或分析战队打法
- 智能采样改为前期最多8秒、中期最多5秒、后期最多3秒，并每2秒低清扫描、按像素变化补帧
- 新增全程3秒和自定义固定间隔两种备选模式
- 一次可多选最多100段录像，自动生成不覆盖旧数据的编号并逐段处理
- 批量队列逐段显示进度、系统编号、帧数和错误，单段失败不会阻断整批

**验证到什么程度**

- ✅ 38个Python测试全过，新增样本边界、权重、阶段密度和编号防覆盖测试
- ✅ 控制台JavaScript语法检查通过
- ✅ 两段18秒合成MP4浏览器批量实测：自动建2场，每场3张基础帧+1张变化补帧
- ✅ 元数据正确保存为“职业选手单排”，完整录像没有进入数据目录

**刻意没做**

- 画面变化检测不是行为理解；目前不能仅凭像素判断团战、卡线、埋伏或反野
- 初始训练权重只落档，不声称1.5/1.2等默认值已经被实验验证
- 仍未开始训练模型，必须先积累并核对真实样本

## 2026-09-08 · v0.2.0：本地拖拽录像、低占用抽帧与双击启动

**问题**

旧控制台要求用户手填录像完整路径，无法拖拽；下载的虎牙完整录像既慢又占空间。
把整段录像再上传给本地服务只会制造第二份大文件，也不能解决问题。

**实现**

- 控制台新增录像拖拽/文件选择区、视频预览、起止时间、抽帧间隔、进度和停止按钮
- 浏览器直接用本机硬件解码，按默认30秒间隔生成宽度不超过1280的 JPEG
- 本地服务只接收单张帧图，拒绝超过8 MB、伪造图片格式和异常时间戳
- 录像本体不进入数据目录；只记录文件名、时长、分辨率和原始大小
- 支持从 iCloud Drive、Dropbox、OneDrive 同步文件夹选择录像，不使用云API
- 保留 ffmpeg 文件路径作为浏览器不支持格式的兼容方式
- 新增 `启动王者实验室.command`，Mac 双击即可启动并打开网页
- 零基础使用说明已改为拖拽流程，并解释云端文件的实际工作方式

**验证到什么程度**

- ✅ 33 个 Python 测试全过
- ✅ 控制台 JavaScript 语法检查通过
- ✅ 4秒合成 MP4 完整网页实测：选择 → 本地解码 → 保存4张JPEG
- ✅ 数据目录仅产生4张帧图和元数据，没有产生整段录像副本
- ✅ 启动脚本语法检查通过

**刻意没做**

- 没有把录像上传到第三方云；云存储由用户已有的同步盘负责，分析只在本机进行
- 没有声称浏览器能播放所有格式；不支持的格式继续走 ffmpeg 兼容模式
- 没有开始 OCR；仍需先用真实比赛产出人工标注作为准确率标尺

## 2026-09-08 · 修复默认数据目录误选系统 `/var`

**问题**

从一个尚未创建 `data/` 的项目目录运行时，旧逻辑会一路向文件系统根部查找
`data / Data / storage / var`。在 macOS 上它会把系统 `/var`（实际为
`/private/var`）误认为项目数据目录，导致真实标注可能写错位置或直接写入失败。

**修复**

- 向上查找在 Git/源码项目根目录、用户主目录和系统顶层目录设置安全边界
- 找不到已有数据目录时，回退到项目根目录的 `data/`
- 保留 `--data` 和 `KPLAB_DATA` 的优先级
- 新增 3 条数据目录回归测试
- README 与交接文档的预期测试数更新为 30

**验证到什么程度**

- ✅ 30 个测试全过
- ✅ 在当前仓库运行 `python3 -m kplab where`，返回仓库内的 `KPL/data`
- ✅ 代码格式检查无尾随空格或冲突标记
- ❌ 尚未创建真实数据目录，未写入任何比赛数据

**刻意没做**

- 没有顺带安装 OCR 或改动标注流程；本次只封住数据落错位置的风险

## 2026-09-08 · 立项：吸取 LOL 项目的经验，搭建王者荣耀决策分析系统

**背景**

已有仓库 `ianzhang141-pixel/Riot`（LOL PC 决策实验室）做到约 18%，
数据链路打通但模型未开始。本仓库是它的姊妹项目，目标相同、数据来源完全不同。

**最重要的一个判断**

**LOL 有 Riot 官方 API，王者荣耀没有公开的对局数据接口。**
所以能继承的是纪律，不是代码 —— 详见
[docs/为什么王者荣耀不能照抄LOL.md](docs/为什么王者荣耀不能照抄LOL.md)。

数据从「请求来的事实」变成「看录像推断出来的猜测」，
由此产生了 LOL 那边不需要的三个设计：

1. **每个字段带来源和置信度**（`schema.py`）——
   识别结果不能和人工核对过的数字长得一样
2. **「没读到」必须是 None 不是 0** ——
   写成 0 会产生一个巨大的假经济差，而且看起来完全正常
3. **数据质量本身要被测量**（`evaluate.py`）——
   拿人工标注当标尺量 OCR 准确率，没标尺就没有准确率

**从 LOL 项目直接继承的教训（对应它踩过的坑）**

| LOL 那边的问题 | 这边怎么做的 |
| --- | --- |
| ① 原始 Timeline 从不落盘，改定义就要全部重下 | 观测只追加不改写，改 State 定义只需重跑，不用重看录像 |
| ② State 只有 1 名玩家 | 从一开始就是 10 名玩家的完整结构 |
| ③ 先锋/幼虫被算进大龙 | 暴君/黑暗暴君/主宰/风暴龙王/先知主宰 **五个独立计数器**，故意不合并 |
| ④ Q(s,a) 是观测性拟合不是因果 | 一行都没写，坑先标好 |
| 路线图里「gzip 是必做项」是返工 | 从第一天就 gzip |
| 「不要夸大验证程度」 | 全部标为「合成数据验证」，零真实数据 |

**新增**

命令行 `python3 -m kplab`：where / doctor / new / frames / check / state / evaluate / rules / serve
网页控制台（8020 端口，和 LOL 的 8000、8010 错开）：控制台 / 标注页 / 逐帧核对页

**验证到什么程度**

- ✅ 全部模块用**合成数据**跑通，命令行和 8 个 HTTP 接口手工验证
- ✅ 三条关键性质写测试确认：无未来信息泄漏、五种资源不合并、缺失不等于 0
- ✅ 路径穿越防护（`/frame` 接口）已验证
- ❌ **零真实数据** —— 一场真实的王者荣耀比赛都没进过这个系统
- ❌ OCR 准确率未知（没有 ground truth 就测不了）
- ❌ HUD 内置坐标是**猜的**，明确标为未标定
- ❌ 10 条游戏常量全部标为「待核对」，是凭印象写的默认值

**刻意没做**

- 任何胜率模型 / Q 值 / Regret —— 一行都没有
- 小地图英雄识别 —— 需要模板匹配那条路线，本版本没做。
  **这是最大的技术缺口**：没有位置就回答不了「入侵值不值」
- HUD 标定的拖框界面（只有后端接口）
- 自动把 OCR 结果写进观测 —— 在准确率被测出来之前，不给这条路开口子

**一次变异测试暴露的盲点（方法论，值得记下来）**

27 条测试写完后全过，但「全过」本身不说明测试有效。
于是故意做了三次破坏，看测试抓不抓得到：

| 破坏 | 结果 |
| --- | --- |
| 把黑暗暴君并进暴君（复现 LOL 的 ③ 号 bug） | ✅ 抓到，2 条挂 |
| 允许用后面的帧回头修前面的帧 | ✅ 抓到，1 条挂 |
| 删掉「有一个输入未知则结果未知」的守卫 | ❌ **没抓到，全过** |

第三条是真盲点：`schema.derive()` 的守卫被删掉，测试却全过，
因为 `state.py` 在调用它之前自己也判断了一次。
**契约要在契约所在的那一层测**，否则下一个调用方就会掉进去。
已补 `test_derive_itself_returns_unknown_when_any_input_is_unknown`，
重跑变异确认能抓到。

教训：测试全过不等于测试有效。改了守卫代码，就该把守卫删掉试一次。

**一个自己发现并修掉的 bug**

`parse_clock("2.3万")` 一开始会返回 2 分 03 秒 —— 因为正则允许秒数是 1~2 位，
"2.3" 就匹配上了。计时器的秒数永远是两位（12:34 / 2:05），
已改为强制两位并排除带「万」的文本。这类错最难发现：结果是一个合法的时间，
只是完全不对，会让整场的时间轴错位到别的时刻。
