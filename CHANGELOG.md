# 变更日志

> **给 AI：每次改动都必须在最上面加一条，并同步更新 [项目状态.md](项目状态.md)。**
> 格式：日期 + 做了什么 + 验证到什么程度 + 刻意没做什么。
> 最新的在最上面。

---

## 2026-09-08 · v0.3.0：样本分层、阶段加密采样与批量录像处理

**问题**

- 职业正式比赛数量有限，不能把它当成唯一录像来源
- 职业比赛、职业选手排位和其他高端局分布不同，混在一起会污染战队打法分析
- 固定30秒抽帧会漏掉完整团战、反野、卡线和埋伏过程
- 每段录像先手工建一场记录，无法规模化导入

**实现**

- 新增职业正式比赛、职业选手单排、其他顶尖选手、训练赛、其他录像五层标签
- 每场保留选手、战队、游戏版本、分段、赛事、自定义标签和可调训练权重
- 职业正式比赛带独立稳定标记，可供后续提高权重或分析战队打法
- 智能采样改为前期最多8秒、中期最多5秒、后期最多3秒，并每2秒低清扫描、按像素变化补帧
- 新增全程3秒和自定义固定间隔两种备选模式
- 一次可多选最多100段录像，自动生成不覆盖旧数据的编号并逐段处理
- 批量队列逐段显示进度、系统编号、帧数和错误，单段失败不会阻断整批

**验证到什么程度**

- ✅ 38个Python测试全过，新增样本边界、权重、阶段密度和编号防覆盖测试
- ✅ 控制台JavaScript语法检查通过
- ✅ 两段18秒合成MP4浏览器批量实测：自动建2场，每场3张基础帧+1张变化补帧
- ✅ 元数据正确保存为“职业选手单排”，完整录像没有进入数据目录

**刻意没做**

- 画面变化检测不是行为理解；目前不能仅凭像素判断团战、卡线、埋伏或反野
- 初始训练权重只落档，不声称1.5/1.2等默认值已经被实验验证
- 仍未开始训练模型，必须先积累并核对真实样本

## 2026-09-08 · v0.2.0：本地拖拽录像、低占用抽帧与双击启动

**问题**

旧控制台要求用户手填录像完整路径，无法拖拽；下载的虎牙完整录像既慢又占空间。
把整段录像再上传给本地服务只会制造第二份大文件，也不能解决问题。

**实现**

- 控制台新增录像拖拽/文件选择区、视频预览、起止时间、抽帧间隔、进度和停止按钮
- 浏览器直接用本机硬件解码，按默认30秒间隔生成宽度不超过1280的 JPEG
- 本地服务只接收单张帧图，拒绝超过8 MB、伪造图片格式和异常时间戳
- 录像本体不进入数据目录；只记录文件名、时长、分辨率和原始大小
- 支持从 iCloud Drive、Dropbox、OneDrive 同步文件夹选择录像，不使用云API
- 保留 ffmpeg 文件路径作为浏览器不支持格式的兼容方式
- 新增 `启动王者实验室.command`，Mac 双击即可启动并打开网页
- 零基础使用说明已改为拖拽流程，并解释云端文件的实际工作方式

**验证到什么程度**

- ✅ 33 个 Python 测试全过
- ✅ 控制台 JavaScript 语法检查通过
- ✅ 4秒合成 MP4 完整网页实测：选择 → 本地解码 → 保存4张JPEG
- ✅ 数据目录仅产生4张帧图和元数据，没有产生整段录像副本
- ✅ 启动脚本语法检查通过

**刻意没做**

- 没有把录像上传到第三方云；云存储由用户已有的同步盘负责，分析只在本机进行
- 没有声称浏览器能播放所有格式；不支持的格式继续走 ffmpeg 兼容模式
- 没有开始 OCR；仍需先用真实比赛产出人工标注作为准确率标尺

## 2026-09-08 · 修复默认数据目录误选系统 `/var`

**问题**

从一个尚未创建 `data/` 的项目目录运行时，旧逻辑会一路向文件系统根部查找
`data / Data / storage / var`。在 macOS 上它会把系统 `/var`（实际为
`/private/var`）误认为项目数据目录，导致真实标注可能写错位置或直接写入失败。

**修复**

- 向上查找在 Git/源码项目根目录、用户主目录和系统顶层目录设置安全边界
- 找不到已有数据目录时，回退到项目根目录的 `data/`
- 保留 `--data` 和 `KPLAB_DATA` 的优先级
- 新增 3 条数据目录回归测试
- README 与交接文档的预期测试数更新为 30

**验证到什么程度**

- ✅ 30 个测试全过
- ✅ 在当前仓库运行 `python3 -m kplab where`，返回仓库内的 `KPL/data`
- ✅ 代码格式检查无尾随空格或冲突标记
- ❌ 尚未创建真实数据目录，未写入任何比赛数据

**刻意没做**

- 没有顺带安装 OCR 或改动标注流程；本次只封住数据落错位置的风险

## 2026-09-08 · 立项：吸取 LOL 项目的经验，搭建王者荣耀决策分析系统

**背景**

已有仓库 `ianzhang141-pixel/Riot`（LOL PC 决策实验室）做到约 18%，
数据链路打通但模型未开始。本仓库是它的姊妹项目，目标相同、数据来源完全不同。

**最重要的一个判断**

**LOL 有 Riot 官方 API，王者荣耀没有公开的对局数据接口。**
所以能继承的是纪律，不是代码 —— 详见
[docs/为什么王者荣耀不能照抄LOL.md](docs/为什么王者荣耀不能照抄LOL.md)。

数据从「请求来的事实」变成「看录像推断出来的猜测」，
由此产生了 LOL 那边不需要的三个设计：

1. **每个字段带来源和置信度**（`schema.py`）——
   识别结果不能和人工核对过的数字长得一样
2. **「没读到」必须是 None 不是 0** ——
   写成 0 会产生一个巨大的假经济差，而且看起来完全正常
3. **数据质量本身要被测量**（`evaluate.py`）——
   拿人工标注当标尺量 OCR 准确率，没标尺就没有准确率

**从 LOL 项目直接继承的教训（对应它踩过的坑）**

| LOL 那边的问题 | 这边怎么做的 |
| --- | --- |
| ① 原始 Timeline 从不落盘，改定义就要全部重下 | 观测只追加不改写，改 State 定义只需重跑，不用重看录像 |
| ② State 只有 1 名玩家 | 从一开始就是 10 名玩家的完整结构 |
| ③ 先锋/幼虫被算进大龙 | 暴君/黑暗暴君/主宰/风暴龙王/先知主宰 **五个独立计数器**，故意不合并 |
| ④ Q(s,a) 是观测性拟合不是因果 | 一行都没写，坑先标好 |
| 路线图里「gzip 是必做项」是返工 | 从第一天就 gzip |
| 「不要夸大验证程度」 | 全部标为「合成数据验证」，零真实数据 |

**新增**

命令行 `python3 -m kplab`：where / doctor / new / frames / check / state / evaluate / rules / serve
网页控制台（8020 端口，和 LOL 的 8000、8010 错开）：控制台 / 标注页 / 逐帧核对页

**验证到什么程度**

- ✅ 全部模块用**合成数据**跑通，命令行和 8 个 HTTP 接口手工验证
- ✅ 三条关键性质写测试确认：无未来信息泄漏、五种资源不合并、缺失不等于 0
- ✅ 路径穿越防护（`/frame` 接口）已验证
- ❌ **零真实数据** —— 一场真实的王者荣耀比赛都没进过这个系统
- ❌ OCR 准确率未知（没有 ground truth 就测不了）
- ❌ HUD 内置坐标是**猜的**，明确标为未标定
- ❌ 10 条游戏常量全部标为「待核对」，是凭印象写的默认值

**刻意没做**

- 任何胜率模型 / Q 值 / Regret —— 一行都没有
- 小地图英雄识别 —— 需要模板匹配那条路线，本版本没做。
  **这是最大的技术缺口**：没有位置就回答不了「入侵值不值」
- HUD 标定的拖框界面（只有后端接口）
- 自动把 OCR 结果写进观测 —— 在准确率被测出来之前，不给这条路开口子

**一次变异测试暴露的盲点（方法论，值得记下来）**

27 条测试写完后全过，但「全过」本身不说明测试有效。
于是故意做了三次破坏，看测试抓不抓得到：

| 破坏 | 结果 |
| --- | --- |
| 把黑暗暴君并进暴君（复现 LOL 的 ③ 号 bug） | ✅ 抓到，2 条挂 |
| 允许用后面的帧回头修前面的帧 | ✅ 抓到，1 条挂 |
| 删掉「有一个输入未知则结果未知」的守卫 | ❌ **没抓到，全过** |

第三条是真盲点：`schema.derive()` 的守卫被删掉，测试却全过，
因为 `state.py` 在调用它之前自己也判断了一次。
**契约要在契约所在的那一层测**，否则下一个调用方就会掉进去。
已补 `test_derive_itself_returns_unknown_when_any_input_is_unknown`，
重跑变异确认能抓到。

教训：测试全过不等于测试有效。改了守卫代码，就该把守卫删掉试一次。

**一个自己发现并修掉的 bug**

`parse_clock("2.3万")` 一开始会返回 2 分 03 秒 —— 因为正则允许秒数是 1~2 位，
"2.3" 就匹配上了。计时器的秒数永远是两位（12:34 / 2:05），
已改为强制两位并排除带「万」的文本。这类错最难发现：结果是一个合法的时间，
只是完全不对，会让整场的时间轴错位到别的时刻。
