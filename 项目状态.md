# 项目状态（AI 交接文档）

> **给下一个 AI（GPT / Claude / 其他）：先读这一份，再动手。**
> 这里写的是「现在到底是什么状态」，不是计划。每次改动后必须更新这份文档和
> [CHANGELOG.md](CHANGELOG.md)。
>
> 最后更新：2026-09-09（首条虎牙真实长回放已接入并分场）

---

## 总进度：约 21%

详见 [路线图.md](路线图.md)。

**不要因为「代码写完了」就以为项目走了一半。**
现在的状态是：一整套工具做好了，**首条2小时36分虎牙真实回放已完成侦察并划出8场比赛**。
高密度帧正在生成；人工标注、可信OCR数据和模型仍然没有。21% 主要代表采集与核对工具更完整，
不代表模型进度。

第二轮两边并行做了三件事：
Claude 把「小地图识别」从 0% 推到约 45%（认位置了，但**认不出英雄**）并补上 HUD 标定页；
GPT 做了浏览器拖拽录像、批量智能抽帧、样本分层、网页来源解析、远程流式读取、S44经济参考库和通识参考库。

## 一句话现状

工具链（录像来源 / 流式读取 / 存储 / 样本分层 / 批量智能抽帧 / State / 校验 / 标注 / 评估 / 网页控制台 / 命令行）已写完并用合成数据跑通，
**已有首批真实录像帧，但零人工真值、零模型**。
当前卡点：8场高密度抽帧完成后，从第1场人工标注10~15帧，把整条链路走一遍。

---

## 零、接手的前五分钟：先跑这一条

```bash
python3 -m unittest discover -s tests -v
```

不需要装任何东西，应该看到 **162 个测试全过**。

**这不是走形式，是这个项目的安全带。**
本项目最危险的三种错误 —— 把「没读到」写成 0、用后面的帧回头修前面的帧、
把暗影暴君算进暴君 —— **全都不会报错**。它们能让代码正常跑、让报表更好看、
让曲线更漂亮，只是把数据悄悄弄坏，几个月后才发现，而那时已经分不清
是模型的问题还是输入本来就是错的。

所以这三条被测试钉死了。**任何一条挂了，不要改测试让它通过** ——
那说明你的改动本身是错的。

### 然后按这个顺序读（约 20 分钟）

| 顺序 | 文档 | 为什么 |
| --- | --- | --- |
| 1 | [docs/为什么王者荣耀不能照抄LOL.md](docs/为什么王者荣耀不能照抄LOL.md) | 不读这个会想当然地照抄 LOL 的做法 |
| 2 | 本文档剩下的部分 | 现在是什么状态、什么没做 |
| 3 | `kplab/schema.py` 开头的注释 | 本项目的核心设计（值+来源+置信度）就这一个文件 |
| 4 | `tests/test_invariants.py` | 三条不许违反的性质，以及为什么 |
| 5 | [路线图.md](路线图.md) 第四节 | 那张标注工时表决定了整个项目的技术路线 |

代码只有 `kplab/` 一个包，20 个 Python 文件，对照表在第三节。

---

## 一、先读这个

[docs/为什么王者荣耀不能照抄LOL.md](docs/为什么王者荣耀不能照抄LOL.md)

它解释了本项目和 LOL 那个仓库（`ianzhang141-pixel/Riot`）的根本差异：
**LOL 有 Riot 官方 API，王者荣耀没有。**
数据从「请求来的事实」变成「看录像推断出来的猜测」，
这一条改变了整个系统的形状，不是难度问题。

⚠️ 上面那句「王者荣耀没有公开接口」是按我所知写的，**没有联网核实过**。
如果用户知道腾讯开放了什么接口，整个设计要重新考虑。

---

## 二、环境事实（最容易被误判的部分）

和 LOL 项目完全一样，照抄过来：

| 事实 | 说明 |
| --- | --- |
| 用户的 Mac | 所有真实数据、录像都在这里。`127.0.0.1:8020` 指这台机器 |
| 云端 AI（Claude Code） | 跑在云容器里，**访问不到用户的 localhost 和硬盘**。本机结果只能由用户贴回来 |
| 用户本人 | **完全没有编程基础**。操作指导必须精确到「按哪个键、看到什么算成功」 |

**推论：** 不要给用户 `127.0.0.1` 的链接让 AI 去看，AI 看不到。
不要假设用户知道路径、Shell、端口、JSON、Git 是什么。

端口分配（三个可以同时开）：
`8000` = LOL 的 GPT 网站，`8010` = LOL 的 lolab，`8020` = 本项目 kplab。

---

## 三、已经做完的（用合成数据验证过）

| 模块 | 作用 | 验证方式 |
| --- | --- | --- |
| `paths.py` | 定位 data 目录 | 合成 |
| `rules.py` | 游戏常量 + **「待核对」标记** | 合成 |
| `season_s44.py` | S44官方说明、最高资料优先级、歧义条目与可计算条目隔离 | 用户提供的官方说明 + 合成校验 |
| `economy_s44.py` | 32条非官方经济参考、4处冲突按官方说明停用、6条带来源估算公式 | 用户文档 + 公式测试 |
| `knowledge_s44.py` | 20条个人通识、术语归一化、角色说法降级和版本快照隔离 | 用户文档 + 隔离测试 |
| `schema.py` | **值 + 来源 + 置信度** 的三元组结构 | 合成 |
| `store.py` | gzip 落档，观测只追加不改写 | 合成 |
| `samples.py` | 职业比赛/职业选手单排/顶尖玩家等样本分层与初始权重 | 合成 + 网页实测 |
| `video.py` | 阶段加密采样 + 画面变化补帧 + 浏览器本地抽帧（不复制整段录像） | 双录像批量网页实测 |
| `sources.py` | 网页/直链/本地来源识别、Resolver、来源批次、重复检测、缓存清理 | 合成 + HTTP流式实测 |
| `hud.py` | HUD 区域标定（未标定就拒绝识别） | 合成 |
| `ocr.py` | 可插拔识别后端 + 数字解析 | 合成 |
| `state.py` | 观测 → 分钟级 State，**严格前向** | 合成 |
| `check.py` | 完整性 / 规则违反 / 可信度 三层检查 | 合成 |
| `annotate.py` | 人工标注（零依赖） | 合成 |
| `evaluate.py` | 人工标注 vs 机器识别的准确率 | 合成 |
| `server.py` + 五个网页 | 控制台 / 标注页 / 标定页 / 逐帧核对 / **比赛分析** | 浏览器实测 |
| `__main__.py` | 命令行 | 手工验证 |

### 2026-09-08 数据目录安全修复

已修复默认数据目录可能误选 macOS 系统目录 `/private/var` 的问题。
现在向上查找会在 Git/源码项目根目录、用户主目录或系统顶层目录前停止；
找不到已有目录时回退到项目自己的 `data/`。新增 3 条测试，分别覆盖：
项目内回退、不越界选择祖先的 `var/`、命令行明确指定目录保持最高优先级。

### 2026-09-08 本地录像低占用导入

控制台已增加拖拽/点击选择录像入口。浏览器在本机解码，只把按间隔抽出的
JPEG 帧发给 `127.0.0.1` 保存，不复制整段录像；支持从 iCloud Drive、Dropbox、
OneDrive 等同步文件夹选择文件，不需要云存储 API。仓库根目录新增 Mac 双击启动文件。

已用 4 秒、640×360 的合成 MP4 完整验证：网页建立比赛 → 选择录像 → 每秒抽帧 →
保存 4 张 JPEG → 比赛清单显示帧数。数据目录中只有 4 张帧图和 `meta.json`，没有录像副本。

### 2026-09-08 样本分层与批量智能采样

控制台现在可以一次选择最多100段录像并自动逐场建档。职业正式比赛、职业选手
排位和其他顶尖玩家对局不会混成同一来源；元数据保留游戏版本、选手、战队、
分段、赛事、自定义标签和可调训练权重。

默认智能采样为前期8秒、中期5秒、后期3秒的基础帧最大间隔，同时每2秒做一次
低清画面扫描，在变化明显的区间补帧。已用两段18秒合成MP4做完整浏览器批量实测：自动建2场，每场保存
3张基础帧和1张变化补帧，没有完整录像副本。变化补帧不是语义识别，不能据此
声称已经理解团战、埋伏或反野。

### 2026-09-08 S44赛季规则库

用户提供并明确声明为官方说明的当前赛季规则已拆成26条结构化记录：21条数值明确
的规则允许进入自动校验，5条官方原文歧义或数值不完整的规则只作记录。核心时间已接入校验：暴君/主宰
4分钟、暗影暴君/暗影主宰10分钟、风暴龙王20分钟、红蓝BUFF 30秒。

来源身份记录为“官方说明（用户提供）”，并保留未附原始公告链接、未独立外部核验的状态。边路兵线“180分钟”、一塔已损生命护盾、
暗影暴君具体加强数值、20分钟兵线具体成长、红隼“25～20”金币规则均保留为
待澄清，不参与自动计算。

### 2026-09-08 网页录像来源与流式分析

控制台新增“添加网络录像来源”。支持多网址粘贴、TXT/CSV、直接MP4、m3u8、
B站、虎牙、斗鱼和通用网页。网页由可替换的 yt-dlp Resolver 解析；本地与远程
最终都进入统一视频读取层，不要求CV模块知道录像来自哪个平台。

远程任务只长期保存 `originUrl`。带签名的真实媒体地址、Cookie和请求头在任务
真正执行时重新获取，只在内存中传给 ffmpeg。网络或地址过期最多重试3次；单项
失败不会阻断同批次后续任务。系统不生成完整远程录像副本，只保存稀疏证据帧。

已用12秒HTTP录像和8秒HLS/m3u8完成真实流式验证：均为一次连续解码，分别保存
6张、4张JPEG，且没有完整录像副本。
匿名B站页面测试被平台以HTTP 412拒绝，系统明确显示访问受限；尚未把这次失败
冒充“平台已完整实测通过”。

### 2026-09-09 S44非官方经济机制参考库

已完整读取用户提供的7页《S44王者经济机制》及其中45张截图，整理为32条结构化
参考记录。资料明确是业余实测，因此全部条目禁止进入硬性合法性校验或充当训练真值；
只有开局金币、自然金币、多人经济分配、兵线时间、防御塔金币和基础人头金币6条
内部完整公式允许以0.55默认置信度进行带来源估算。

4处与官方说明冲突的非官方内容均已停用：普通暴君重生240/210秒、前4分钟中路2+2/1+2兵线、
二塔视野之灵120/20秒刷新、红隼队友固定20金币/“25～20”。前三项采用官方
说明，红隼金币继续保持不可计算。文档中的15至25级数据判为训练营
或指挥官模式范围，不进入标准S44比赛模型。

### 2026-09-09 S44个人通识参考库

已完整读取用户提供的4页《S44王者荣耀通识》及24张截图，整理为20条结构化
参考记录。7条基础术语可以0.4置信度辅助人工标注名称归一化，但全部20条都禁止
作为硬性校验、自动模型标签或训练真值。

“打野必须带惩击和打野装”“中路必须是法师”“游走必须买学识宝石”三条绝对
说法已经降级为多证据提示；实际分路必须结合线路位置、资源获取和支援路线判断。
2026-09-07英雄热度榜注明“算法测试中”，与装备、召唤师技能和惩击数值一并作为
版本快照隔离，不录入固定英雄强度、分路或补丁事实。

### 三条已用测试确认成立的性质

1. **无未来信息泄漏** —— 第 6 帧的坏读数不会回头改第 5 帧（已验证）
2. **五种中立资源不合并** —— 暴君/暗影暴君/主宰/暗影主宰/风暴龙王各自独立（已验证）
3. **缺失不等于 0** —— 红方经济未知时，经济差是「未知」而不是一个假的正数（已验证）

---

## 四、明确没做的（不要误以为有）

| 东西 | 状态 |
| --- | --- |
| V(s) 胜率模型（代码 + 训练拒绝条件） | ✅ 已做，但**没有任何训练好的模型** |
| 胜率曲线 | ⚠️ 有界面有曲线，但曲线是**假设基线**不是模型输出 |
| 行为抽象 | ❌ 没有（只定义了事件类型，那不是行为） |
| Q(s,a) / 分支胜率 | ❌ 没有，界面上明确标为「未实现」 |
| Regret / 归因 | ❌ 没有 |
| 小地图**认出位置** | ✅ 已做（`minimap.py`，合成图 10/10，误差 <0.012） |
| 小地图**认出是哪个英雄** | ❌ **没有** —— 只知道「这里有个蓝方标记」 |
| **图标变化 → 事件**（推塔/打龙/击杀） | ✅ 已做（`events_cv.py`），但地标要人工标定 |
| 中立资源**是谁拿的** | ✅ 已做（`announce.py` 从播报判队伍），团战区间除外 |
| 真实录像跑通 | ❌ 从未 |
| OCR 准确率 | ❌ 未知（没有 ground truth 就没法测） |
| HUD 标定**界面** | ✅ 已做（`/calibrate` 拖框标定 + 小地图阈值实时调试） |
| HUD 标定**结果** | ❌ 内置坐标仍是**猜的**，没人对着真画面标过 |
| 小地图颜色阈值 | ❌ 是凭经验写的起点，未用真实画面标定 |
| 游戏常量 | ✅ S44官方说明已录入26条，其中21条可计算、5条因官方原文歧义暂不计算 |

### 小地图这个缺口现在只补了一半

**做到了：** `minimap.py` 能认出小地图上有几个蓝色标记、几个红色标记、各在哪。
不装任何 Python 包 —— 让 ffmpeg 把画面区域吐成原始 RGB 字节，
Python 那边只是读 bytes 加一个纯手写的连通域标记。合成图上 10/10，误差 <0.012。

**没做到：** 认不出那个蓝点**具体是哪个英雄**。区分英雄要头像模板库、
要处理遮挡和缩放，是另一个量级的工作。

**所以现在的位置信息是这样的：**
「上半区有 3 个蓝方标记」是可信的（阈值标定之后），
「1 号位在 (0.3, 0.4)」是猜的 —— `to_observation_players()` 按位置排序
硬塞进槽位，置信度只给 0.15，远低于可信线 0.75。

**这意味着「入侵野区值不值」还是答不了**，因为那需要知道敌方打野这个**具体的人**在哪。
但「敌方半区出现了几个人」这类问题已经能答了 —— 先把能做的做扎实。

---

## 四·四、事件检测：不读文字，读图标

**这是用户提供的关键思路，比 OCR 那条路好得多。**

| 事件 | 画面上的表现 |
| --- | --- |
| 推掉防御塔 | 小地图上那座塔的图标**消失** |
| 拿到暴君 / 主宰 | 小地图上那个坑的图标**消失** |
| 英雄阵亡 | 头像栏里那个头像**变灰** + 复活倒计时 |
| （佐证）击杀 | 右上角比分**加一** |

「某个固定位置的图标还在不在」比「这串像素是几」容易一个数量级。

### 两条必须守住的规矩

**① 塔是单向的，中立资源是周期的。**
塔的图标消失了就永远不回来；暴君会刷新，消失又出现是完全正常的。
两者用同一套逻辑会得到「暴君被拆了 6 座」—— 换个地方重犯 LOL 那个
「先锋算成大龙」的错。所以 `TowerTracker` 和 `ObjectiveTracker` 是两个类，
刻意不合并。塔的图标如果「回来了」，记为矛盾并把整场判为不可信。

**② 两个独立信号对上了才算击杀。**
头像变灰和比分加一来自画面上不同的两块区域，出错原因也不同：

- 两个都动 → 高置信度，且知道是谁死的
- 只有比分动 → 确实死了人，但不知道是谁（只记团队数）
- **只有头像变灰 → 可疑**（特效遮挡也会变灰），不当击杀

### 归属：用播报补上（用户提的第二个关键思路）

图标消失只说明资源死了，说不出是谁打的。**但播报能说。**

推塔、击杀、开龙都会在屏幕中间上方弹一条横幅并停留几秒。所以：

    粗扫（每 30~60 秒一帧）
      → 发现「t=300 图标没了，t=240 还在」
      → 事件必定发生在 (240, 300]
      → **只在这 60 秒里精抽帧**（每 1.5 秒一张，约 41 帧）
      → 在播报区找横幅 → 看横幅偏蓝还是偏红 → 得出归属

**关键在于只精抽这一小段。** 整场 20 分钟按 1.5 秒抽是 800 帧，
但每个事件只需 40 帧，一场十几个事件也就几百帧。

只判队伍不读文字 —— 读文字要回到 OCR，那条路没这个可靠，
而 State 里的计数器本来就是按队伍记的。

**三种情况仍然是「不知道」：**
区间里没找到横幅、横幅颜色分不出偏向、
以及**一个区间里挤了多个事件**（团战：三个人头 + 一条龙共用一个区间，
那条播报属于哪个根本判断不了）。第三种置信度减半到 0.375，
**正好过不了 0.5 的写入门槛**，所以不会污染数据。

---

## 四·五、关于「分析界面」的现状（用户最关心的一块）

用户问的是：**上传训练赛录像 → 出胜率曲线 → 点拐点 → 看分支胜率和分析。**

现在这条链路**长成了，但中间有一段是假的**，必须说清楚：

| 环节 | 状态 |
| --- | --- |
| 上传录像 / 抽帧 | ✅ 能用（GPT 做的浏览器拖拽 + 智能抽帧） |
| 帧 → 观测 | ⚠️ 人工标注能用；自动识别没标定过 |
| 观测 → State | ✅ 能用 |
| State → 胜率曲线 | ⚠️ **曲线是假设出来的，不是模型算的** |
| 曲线 → 拐点加粗圆点 → 点击 | ✅ 能用，`/analysis` 页面 |
| 拐点 → 「发生了什么」 | ✅ 能用，从数据如实读出的事实 |
| 拐点 → **分支胜率** | ❌ **未实现**，界面上明确标着 |

### 那条曲线为什么是假的

没有真实数据就训不出模型。两条路：什么都不显示，或者先用一条
**公开承认是假设**的曲线把链路跑通。选了后者，理由和 LOL 路线图 AI2 一样：
在花几个月采数据之前先看到产品长什么样，是最降风险的一步。

**但防线是硬的**（`model.py` 开头有完整说明）：
曲线带 `kind` 字段、基线不给准确率、页面顶部有大号警告横幅、线画成虚线、
`train()` 数据不够直接拒绝且不会悄悄退回基线。
这七条都有测试钉住，并做过变异测试确认。

### 分支胜率为什么不做

那是 Q(s,a)，而且必须是**因果**估计。
简单拟合「做了动作 A 的人后来赢了多少」会学到「优势方才有余裕做 A」——
LOL 项目里 GPT 那版就是这么做的，用户当时明确警告过。
编一个「如果当时不开团，胜率 61%」出来看起来正是用户想要的，
而且没有任何一步会报错 —— **这是整个项目最容易骗到人的地方**，所以留空。

---

## 五、下一步优先级（不要跳步）

### 你0 · 挑一场比赛，人工标 10~15 帧 【你】· 约 40 分钟 ← **现在卡在这**

```
python3 -m kplab serve
```
然后在网页上：建立比赛 → 打开标注页 → 边看录像边填。
**不需要装 ffmpeg，也不需要装任何识别工具。**

这一步卡住后面全部工作。它同时产出三样东西：
第一批完全可信的数据、评估 OCR 用的 ground truth、以及你对「这活儿有多累」的真实体感
（第三样最重要，它决定整个项目的规模上限）。

### 你1 · 核对 10 条游戏常量 【你】· 约 10 分钟
控制台最下面那张表。现在的值是**凭印象写的**，版本改动后很可能不对，
而错的常量会让 check 的上限校验一起失效。

### AI1 · 根据真实标注体感，重估工作量 【AI + 你】
标完 15 帧你会知道一场要多久。用这个数字重算路线图第四节那张表。
**如果一场要 40 分钟，那么「几万场」这条路就是不通的**，
整个项目要转向「少量高质量样本 + 更强的先验」，而不是照抄 LOL 的大数据思路。

### ~~AI2 · 小地图粗识别~~ ✅ 已做（2026-09-08）
「认颜色和数量」的版本做完了，见上面「小地图这个缺口现在只补了一半」。
**下一步是英雄区分**，那个还没开始，难度未知。

### ~~AI3 · HUD 标定界面~~ ✅ 已做（2026-09-08）
`/calibrate` 页：拖框标定 HUD 区域 + 小地图颜色阈值实时调试
（识别结果会直接画在画面上，超过每方 5 人会当场判为不可用）。
**但没人对着真画面标过**，所以标定结果仍然是空的。

### AI3b · 真跑一次 OCR 【AI】· 需要真实数据
标定页有了，但还没有真实帧图可标。等用户标注完第一场再说。

### 之后 · 一版「丑但是真」的 V(s)
和 LOL 路线图 AI2 同样的思路：用现有的少量数据 + 最简单的模型，
**明知道它不准**，目的是把「State → 胜率 → 曲线 → 界面」整条链路打通，
在投入大量标注之前就看到产品长什么样。这是最降风险的一步。

---

## 六、必须避免的错误（继承自 LOL 项目，一条不少）

1. 不能因为职业选手做了某个动作就认为它是正确答案
2. 不能因为行为后 30 秒胜率下降就判定行为错误（队友、对手、其他线路都会污染因果）
3. 不能只训练赢的比赛（必须包含赢/输/优势被翻/劣势翻盘）
4. 不能泄漏未来信息（**这边还多一条：不许用后面的帧回头修前面的帧**）
5. 职业正式比赛 ≠ 职业选手排位，必须打不同标签分开处理

### 本项目额外的三条

6. **不许把识别结果当事实呈现。** 每个字段都带来源和置信度，
   显示时可信 / 存疑 / 未知必须长得不一样。
7. **不许把「没读到」写成 0。** 那是编造，而且是最难发现的一种。
8. **不许在没有人工标注的情况下相信 OCR。** 没有标尺就没有准确率。

### 改动本项目时的一条硬要求

**改完跑一遍 `python3 -m unittest discover -s tests`。**

如果你改动了 `schema.py` / `state.py` / `store.py` 里的任何逻辑，
光跑通测试还不够 —— 请顺手做一次**变异测试**：
故意把你认为的守卫代码删掉，确认真的有测试挂掉。

这不是多此一举。本项目立项时就靠这个手法抓到过一个盲点：
把 `schema.derive()` 里「有一个输入未知则结果未知」的守卫删掉，
27 条测试竟然**全都还能过** —— 因为 `state.py` 在调用前自己也检查了一遍。
契约要在契约所在的那一层测，否则下一个调用方就会掉进去。
（现已补上 `test_derive_itself_returns_unknown_when_any_input_is_unknown`。）

---

## 七、文档索引

| 文档 | 给谁看 |
| --- | --- |
| [README.md](README.md) | 快速了解仓库 |
| [项目状态.md](项目状态.md) | **AI 交接，先读这个** |
| [docs/为什么王者荣耀不能照抄LOL.md](docs/为什么王者荣耀不能照抄LOL.md) | **AI 和用户都该读**，解释根本差异 |
| [路线图.md](路线图.md) | 进度百分比、剩余工作、依赖顺序 |
| [docs/使用说明.md](docs/使用说明.md) | 用户：分步操作指南 |
| [CHANGELOG.md](CHANGELOG.md) | 每次改了什么、验证到什么程度 |
